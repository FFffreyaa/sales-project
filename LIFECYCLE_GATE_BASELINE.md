# 销售项目生命周期对齐 Gate 补充基线

状态：当前 Demo 后续设计与实现基线
适用范围：销售项目生命周期 S0–S6 的阶段出口、双轨事实汇总、人员确认、审批与系统迁移
原始业务基线：`DP-02钱江电气销售项目与投标领域本体包V1.2双轨版.html`

## 1. 基线治理

1. 原始 V1.2 文件继续作为主业务流权威来源，约束三类场景、SalesProject 聚合、L2.1–L2.9 投标作业、ACT-SPM/ACT-BID 双轨活动、对象血缘、人员边界、接口、证据和 Agent 权限。
2. 原始 V1.2 中 G1–G7 与 S0–S6 未形成一致的生命周期出口映射。本补充基线仅重新定义 Gate 的编号、名称、阶段归属和出口判定，不改写原始文件。
3. 原 G1–G7 中的业务控制不得删除，必须按本文件重新归并到新的 G1–G7。
4. 后续 Demo、数据库、API、页面、测试和说明文档涉及 Gate 时，以本补充基线为准；其他业务内容仍以原始 V1.2 为准。
5. 本补充基线属于显式设计变更，不得描述为原始 V1.2 已经明确的一一对应关系。

## 2. 统一结构

- S0–S6 是销售项目唯一生命周期。
- 销售项目管理轨产生价值、策略、关系、资源、行动和风险等经营事实。
- 投标作业轨及专业系统产生需求、方案、BOM、核价、评审、提交、结果等作业事实。
- 每个 S 阶段只有一个用户可见的出口 Gate；Gate 自动汇总双轨来源事实，不设置重复录入表单。
- Gate 形成唯一阶段迁移结论；任何人员不得直接修改项目阶段。
- 专业确认、经营决策、实际提交和客户回执分别留痕，但同一阶段不重复创建相互竞争的 Gate。

## 3. 生命周期与 Gate 一一对应

| 当前阶段 | 出口 Gate | 核心判定 | 决策/确认责任 | 通过事件与去向 |
| --- | --- | --- | --- | --- |
| S0 待立项 | G1 立项门 | 价值、Owner、采购意向证据、查重和交标日期满足立项要求 | 销售主管 | `SalesProjectEstablished`，进入 S1 |
| S1 立项与策略 | G2 策略批准门 | 客户地图、竞争/赢单策略、伙伴资源与方案启动需求齐备 | 销售主管；重大项目可按规则升级 VP | `StrategyApproved`，进入 S2 |
| S2 需求与方案 | G3 需求方案基线门 | 原始需求可追溯，关键参数完整，技术方案、澄清和偏差形成受控基线 | 技术负责人 | `CustomerRequirementBaselined`，进入 S3 |
| S3 核价评审 | G4 核价授权门 | 方案/BOM/成本一致，利润、底价、交期和风险有授权结论 | 财务/价格授权人 | `CostingSolutionApproved`，进入 S4 |
| S4 定价与投标 | G5 商务决策与提交门 | 专业评审、价格授权、偏差、最终提交包和客户窗口满足投标/报价与提交要求 | 销售主管/投标授权决策人；价格例外返回 G4 | 正式提交并取得有效回执后进入 S5；不投/不报价进入 S5 终止处理 |
| S5 结果与移交 | G6 结果与移交门 | 结果事实有效；赢单基线完成接收，或失标/终止原因完成结构化复盘 | 销售主管 | `Won` 进入 S6；`Lost/Terminated` 结束业务生命周期 |
| S6 赢单后跟踪 | G7 经营关闭门 | 下游权威状态和销售遗留责任达到关闭策略 | 系统汇总＋销售项目 Owner；例外升级主管 | `CommercialProjectClosed` |

## 4. 原 Gate 业务控制重新归并

| 原 V1.2 控制 | 新 Gate 归属 |
| --- | --- |
| 原 G1 立项门 | 新 G1 |
| 原 G2 方案启动门 | 新 G2，并补充 S1 策略批准条件 |
| S2 客户要求完整、关键参数可核价 | 新 G3 |
| 原 G3 核价定价门 | 新 G4 |
| 原 G4 投/不投及授权决策＋原 G5 冻结提交 | 合并为新 G5 的一次用户可见 Gate 流程；决策事实与提交事实分别留痕 |
| 原 G6 结果门 | 新 G6 |
| 原 G7 成交后经营跟踪 | 新 G7，并补充 S6 经营关闭条件 |

L2.1–L2.9 的名称、顺序和专业活动边界不因 Gate 重排而改变。

## 5. Gate 标准流程

1. 业务人员在来源工作区持续记录事实，Gate 不重复填写业务材料。
2. 系统按当前项目、阶段、场景和轮次汇总来源对象、不可变 ID、版本与证据。
3. 硬性条件未满足时 Gate 为 `NOT_READY`，只能查看缺口和责任人，不能提交审批。
4. 项目 Owner 在 `READY` 后发起阶段推进；系统再次服务端校验并冻结提交快照。
5. 指定决策人执行批准、退回或拒绝；Agent 不得审批。
6. Gate 达到 `COMPLETED` 后，生命周期服务自动写入退出事件并迁移 S 阶段。
7. 已批准来源发生重大变化时，相关 Gate 进入 `INVALIDATED` 或 `REVIEW_REQUIRED`，不得覆盖历史结论。

推荐状态：

`DRAFT → NOT_READY → READY → PENDING_APPROVAL → RETURNED/APPROVED → EXECUTING → COMPLETED → INVALIDATED`

G1–G4 通常批准后即可完成；G5 批准后还需实际提交和回执；G6 还需完成移交或复盘；G7 在 S6 持续监控直至关闭。

## 6. G5 单 Gate 规则

1. 前台只显示一个“G5 商务决策与提交门”，只发起一次主要审批。
2. 审批人一次性决定投/不投或报/不报，并授权按当前冻结版本提交。
3. 批准后由投标专员或授权销售员执行提交；执行提交不是第二次审批。
4. 系统分别保存 `BidDecisionMade` 与 `CommercialSubmissionAccepted`，取得有效回执后 G5 才完成。
5. 低于底价、重大偏差、授权失效、提交包与批准版本不一致时不得提交；价格问题返回 G4，其他变化使 G5 重新评审。
6. 不投/不报价不伪造 `BidSubmitted`，而是产生明确的决策事件并进入 S5 完成终止原因和证据。
7. G5提交时冻结当前未关闭项目风险的ID、等级、状态和版本；提交后任一风险发生变化，原决策快照失效，必须退回或重新提交G5。
8. 未关闭的高/重大项目经营风险不另设第二个Gate，也不由系统假关闭。主管若仍决定投/报，必须在同一次G5决策中逐项显式承接，并记录经营依据、控制措施、剩余影响和责任；低/中风险作为决策上下文保留。
9. 风险承接记录绑定风险版本、Gate实例和Gate版本，只证明主管知情并接受该次投/报决策的剩余风险，不替代后续风险处置和Owner关闭证据。

## 6.1 G6 单 Gate 规则

1. L2.9 的 `BidResult` 由投标作业轨形成并回传，销售员或销售主管不得在主系统直接伪造中标/未中标事实。
2. 销售项目 Owner 核对自动汇总来源；失标/终止时负责补充结构化原因、证据、差距、改进行动、责任人和期限，然后发起一次 G6。
3. 销售主管只作一次结果与分支确认，不重复审批需求、方案、价格或提交包。
4. 赢单批准后系统冻结不可覆盖的 `CommercialAwardBaseline`，项目保持 S5；合同 APP 按基线哈希接收并返回可追溯回执后，G6 才完成并迁移 S6。
5. 合同接收是 G6 批准后的受控执行，不是第二个 Gate，也不在销售项目 APP 内执行合同评审。
6. 失标/终止分支不形成中标基线；结构化复盘齐套且 G6 批准后记录 `Lost/Terminated` 与 `Closed`，两类事实分别留痕。

## 7. 系统与人员边界

| 主体 | Gate 中的责任 |
| --- | --- |
| 销售员/项目 Owner | 维护经营事实、协调缺口、发起 Gate，不代替专业人员确认，不直接改阶段 |
| 销售主管/VP | 立项、策略、重大经营决策、G5 商务决策、G6 结果确认和督办 |
| 技术负责人 | 编制 G3 所需的标准化需求、方案、澄清和技术偏差来源 |
| 技术评审人 | 独立评审冻结的 G3 需求与技术方案基线；不得与编制身份自审 |
| 投标专员 | 编制 G5 所需的最终提交包并执行获批后的正式提交 |
| 专业评审人 | 独立评审最终提交包，形成 G5 商务决策的专业输入 |
| 财务/价格授权人 | 形成并批准 G4 的成本、利润、底价和价格例外 |
| 投标专员/授权销售员 | 执行正式提交，保存提交版本、时间、通路和客户回执 |
| 销售项目管理 APP | 保存 S 生命周期、Gate 实例、来源快照、决策、审计并执行阶段迁移 |
| 投标作业 APP/专业系统 | 保存需求、方案、BOM、核价、评审、提交和结果权威事实，通过事件/API 回传 |
| 钉钉 | 可承载通知、待办和审批交互，不作为项目主数据源；结果必须回传 Gate |
| 合同、订单、履约、财务系统 | 向 G6/G7 提供下游权威事实，销售项目 APP 只读引用 |
| TEST ADAPTER | 当前本地模拟外部任务与事件；必须标记 `environment=demo`、`simulated=true` |

## 8. 三类场景

底层使用同一 Gate 编码，界面按场景翻译：

- 客户直接投标：G5 显示“投标决策与提交门”。
- EPC 客户询价：G5 显示“报价决策与通路提交门”；每个 `QuotationRoute` 单独保存报价版本和提交状态，项目级 Gate 汇总当前有效报价批次。
- 客户直接询价：G5 显示“直接报价决策与提交门”。

EPC 场景不得在第一条报价通路提交后自动将整个项目推进到 S5；必须按当前批次的有效通路完成规则汇总判定。

### EPC 多通路统一报价补充规则（2026-08-23确认）

1. 一个最终采购机会只建立一个 `SalesProject`；每家向钱江询价的 EPC 建立一条 `QuotationRoute`，项目预计金额不按通路累加。
2. G4 形成项目级统一授权底价和统一授权报价；`routeScope=ALL-EPC-ROUTES` 或显式通路清单必须覆盖批准时全部有效通路。
3. 各通路默认继承统一授权报价。销售Owner不得直接改价；确有配置、范围、交付、税费或商务条款差异时，必须建立 `EpcPriceException`，由价格授权人批准后仅对该通路生效。
4. 通路例外价不得低于G4授权底价，有效期不得超过项目价格授权有效期；否则必须返回G4重新核价授权。
5. G5冻结当前全部有效通路、各自提交包、专业评审和价格依据。每条通路分别取得客户/平台回执，只有全部冻结通路回执入账后G5执行完成并推进S5。
6. G5冻结后新增通路或变价必须先退回G5；不得用首条回执代表整个EPC报价批次完成。
7. 进入S5后，每条已提交 `QuotationRoute` 分别接收一个 `QuotationRouteResult`。该通路只有在对应EPC中标且明确选用钱江时记为 `won`；EPC未中标或中标但未选用钱江均记为该通路 `lost`。
8. 任一通路尚未取得正式结果时，项目级商业结果保持 `pending`，G6不得提交；全部通路结果齐备后，任一且仅一条通路 `won` 则项目汇总为 `won`，全部通路 `lost` 则项目汇总为 `lost`。
9. 若同时出现两条及以上 `won`，系统不得自行选择“最后回传”或任意通路，而应阻断G6并要求核实授标范围、标段拆分、联合体或合同拆分事实。多合同/多标段是否仍属于同一SalesProject，需业务确认后另行配置。
10. 通路结果是投标作业轨的权威事实；项目级 `CommercialResultNotice` 是销售项目APP的确定性汇总。销售主管在G6确认汇总结果与分支，不逐条重审外部结果。

### G5冻结批次变更控制（2026-08-23确认）

| 当前状态 | 新增/撤回/材料或价格变化 | 系统处理 |
| --- | --- | --- |
| G5尚未提交，或已退回 | 允许按角色维护 | 新增/撤回通路后重新计算G5；价格变化仍走通路例外或返回G4，不直接改价 |
| G5待主管决策 | 禁止修改冻结来源 | Owner提交解冻申请；主管核实原因与证据后批准，G5变为returned，再执行具体变更并重提新版本 |
| G5已批准，但当前版本零正式提交回执 | 允许受控撤销尚未执行的授权 | Owner申请、主管批准；保留原批准历史，取消旧版本提交任务，解冻投标包并将G5退回；具体变更完成后重新冻结和决策 |
| G5当前版本已有任一正式客户/平台回执 | 禁止回滚、删除或改写 | 已提交事实不可撤销。后到询价、剩余通路撤回或变价转入跨批次业务判断；规则确认前系统硬阻断 |
| 全部冻结通路均已正式提交 | 不允许返回S4改写批次 | 按原冻结批次进入S5逐通路跟踪结果 |

补充约束：撤回只适用于尚无正式提交回执的通路，保留原 `QuotationRoute` 并标记为 `withdrawn`；关联未提交包、开放价格例外及未完成外部任务失效。不得在通路页面撤回项目最后一条有效通路；采购机会整体不投/不报必须走G5经营决策。

### S5结果核实与更正规则（2026-08-23确认）

1. 销售项目APP不得允许销售Owner或销售主管直接改写投标作业轨回传的 `QuotationRouteResult`。Owner只能在S5、G6冻结前，针对当前有效结果发起“结果核实”，并提供原因和依据。
2. 核实请求生成独立的 `COMMERCIAL_RESULT_TRACKING` 外部任务；投标作业责任人接受任务后，基于新的正式来源回传更正结果。重复发起同一未完成核实请求时复用原任务，不重复生成任务与审计事件。
3. 更正结果必须引用被更正结果、记录更正原因和新证据。原记录变为 `superseded`，不得物理覆盖或删除；新记录成为该通路唯一 `effective` 结果。
4. 更正完成后系统自动废止旧的项目级汇总结果并基于全部当前有效通路重新汇总。若冲突消失则重新开放G6；若仍有两条及以上 `won`，继续阻断G6。
5. 若多条 `won` 确为真实多标段或多合同授标，不得借“更正”把真实结果改成未赢。系统保持冲突状态，待企业确认项目/标段/合同拆分规则后处理。

## 9. 尚待企业规则配置

以下内容不在本补充基线中臆造固定值，实施前按企业规则配置：

- 项目等级对应的审批层级和金额阈值；
- 各 Gate SLA、升级和代理审批规则；
- G5 不投/不报价的批准权限；
- G5已有正式回执后，后到EPC询价应纳入原项目下一报价批次还是新建项目，以及跨批次结果如何汇总的业务识别规则；
- 多家EPC同时确认选用钱江时，按标段/合同拆分一个还是多个SalesProject的企业判定规则；
- G7 对履约、开票、回款和遗留责任的具体关闭阈值；
- 钉钉与专业系统的真实接口、状态枚举和幂等契约。

## 10. 工程执行约束

1. 先修改领域常量、Gate 定义和阶段迁移策略，再开发 G4–G7 页面和 API。
2. 旧 G3/G4/G5 语义及历史记录不得静默覆盖；演示数据需要显式迁移或标记旧版本。
3. 自动化测试至少覆盖正常通过、退回重提、越权、重复审批、来源变更失效、G5 版本不一致阻断、三类场景翻译和多 EPC 通路汇总。
4. README、业务基线、页面文案、测试适配器和审计日志必须使用同一 Gate 口径。

## 11. Gate 与双轨活动快照（运行规则 V2）

Gate 的硬条件以业务对象及版本为主，活动实例用于核验作业责任、状态和来源系统是否闭环；禁止用“活动已完成”替代需求、方案、成本、授权、提交回执或结果事实。

| Gate | 销售项目管理轨上下文 | 投标作业轨硬活动 | 处理规则 |
| --- | --- | --- | --- |
| G1 | ACT-SPM-01 | — | SPM01须处于进行中或已完成；G1批准后由系统完成该活动 |
| G2 | ACT-SPM-02/03/04 | — | 经营活动持续贯穿后续阶段，状态作为决策上下文；来源对象齐套仍是硬条件 |
| G3 | ACT-SPM-05 | ACT-BID-04A/04B | 需求基线与技术方案/BOM专业活动必须完成，并与当前需求、方案、BOM版本一致 |
| G4 | ACT-SPM-06 | ACT-BID-04C | 核价与价格授权专业活动必须完成，并与当前方案/BOM/核价对象一致 |
| G5 | ACT-SPM-03/05/06 | ACT-BID-05/06 | 组包和专业评审必须完成；G5本身形成投/不投决策，批准后再异步执行ACT-BID-08 |
| G6 | ACT-SPM-07 | ACT-BID-09 | 赢单/失标必须有L2.9结果活动；G5不投/不报分支以G5决策事实替代，不伪造ACT-BID-09 |
| G7 | ACT-SPM-08 | 下游只读事件 | 经营跟踪活动与合同、订单、履约、开票、回款事实共同满足企业关闭策略 |

### 11.4 G7 当前工程边界

当前系统只具备合同基线接收回执，尚不具备订单、履约、验收、开票、回款等下游权威事实，也没有经业务确认的遗留责任和超期例外关闭阈值。因此：

- 进入 S6 时系统创建并启动 ACT-SPM-08，用于承接只读下游状态和销售遗留责任；
- 当前不得开放“销售员手工确认 G7 通过”或“主管凭单次审批关闭”的替代流程；
- G7 必须等下游事实契约、关闭阈值、超期升级和例外审批规则确认后实施；
- 在上述条件齐备前，系统显示受控未开放状态，而不是伪造完整生命周期。

提交 Gate 时必须冻结活动定义编码与版本、活动实例不可变ID、状态、实例版本、权威系统、证据和对象引用。审批前服务端重新计算；硬活动未完成，或冻结后任一活动ID、状态、版本、证据或对象引用变化时，禁止批准并要求重新提交。旧 `LIFECYCLE_GATE_V1` 快照保留历史含义；新提交使用 `LIFECYCLE_GATE_V2_ACTIVITY_SNAPSHOT`，不得静默覆盖。
