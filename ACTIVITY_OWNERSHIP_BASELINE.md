# 双轨活动系统责任边界基线

状态：P0 工程基线
适用范围：销售项目管理 APP、投标作业 APP/专业系统、本地集成测试台
依据：DP-02 V1.2 双轨主业务方案、销售项目生命周期 Gate 补充基线、活动转换详解；当原文件内部口径冲突时，以端到端责任闭环和权威数据边界为裁决原则。

## 1. 不可混用的三个概念

- `S0—S6` 是销售项目生命周期，由销售项目管理 APP 权威管理。
- `ACT-SPM` 与 `ACT-BID` 是两条业务活动轨道，不等同于销售项目阶段。
- `G1—G7` 是阶段转换控制；Gate 汇合双轨事实、冻结输入快照并记录人工决策，但不复制专业作业。

## 2. 系统所有权

| 业务内容 | 权威系统 | 销售项目管理 APP 权限 |
|---|---|---|
| SalesProject、S0—S6 | 销售项目管理 APP | 创建、变更、审计 |
| ACT-SPM-01—08 | 销售项目管理 APP | 创建、执行、状态转换、留证 |
| ACT-BID-01—09、04A/04B/04C | 投标作业 APP/专业系统 | 只读投影、Gate 引用，不代填 |
| 需求、技术方案、设计 BOM | 技术/投标专业系统 | 精确引用版本与完成事件 |
| 核价、价格授权 | 核价定价/授权系统 | 精确引用版本、有效期与授权结果 |
| 投标包、专业评审、正式提交 | 投标作业 APP | 引用冻结版本、阻断项、哈希与回执 |
| Gate 快照与销售阶段决策 | 销售项目管理 APP | 权威管理 |
| 合同、订单、履约、开票、回款 | 下游权威系统 | S6 只读投影并派生销售经营行动 |

## 3. 活动实现规则

1. `ActivityDefinition` 是版本化语义字典；`ACT-BID-04` 是父活动，`04A/04B/04C` 是可执行子活动。
2. `ActivityInstance` 仅承载本系统权威的 ACT-SPM 执行；必须保存 Owner、计划/实际时间、状态、证据、异常、对象引用、Gate 引用和完成事件。
3. 外部 ACT-BID 使用 `ExternalActivityProjection`；保存外部不可变 ID、来源系统、状态、对象版本、证据、阻断和同步时间，不允许销售角色修改专业结论。
4. 活动状态与时效状态分开；禁止根据活动标题或中文关键词推断活动编码。
5. Gate 只读取已持久化的销售活动事实和外部投影事实，并把精确来源 ID 写入不可变提交快照。
6. 测试台与未来真实系统使用同一 Inbox/Outbox 契约；`environment=demo`、`simulated=true` 必须显式可见。
7. 同一外部任务可以形成多个可执行子活动投影，但每个投影必须有稳定外部活动ID：当前技术协同分别形成04A/04B，投标包协同分别形成05/06；不得用一条模糊“已完成”吞并不同完成事件。
8. Gate活动快照只做过程一致性控制，不替代业务对象判定；G3—G6硬活动映射以`LIFECYCLE_GATE_BASELINE.md`第11节为准。

## 4. 专业边界裁决

- 04A 形成需求基线；04B 形成技术方案和报价设计 BOM；04C 只引用已确认的方案/BOM完成核价与价格授权。
- ACT-BID-09 关闭投标作业责任，不关闭销售项目成交后经营责任。
- ACT-SPM-08 和 S6/G7 保留；销售继续跟踪下游履约、开票和回款只读事实。
- ACT-BID-07 与 ACT-BID-08 是两个活动，但销售项目 APP 保持一个 G5：先形成经营决策，再异步完成受控提交。

## 5. ACT-SPM 状态机

- 允许的主路径：`planned → in_progress → completed`。
- 允许的异常路径：`planned/in_progress → blocked → in_progress`，以及开放状态进入 `cancelled`。
- `completed` 与 `cancelled` 是终态，不允许原地改回开放状态；纠错只能追加证据、审计事件或创建新活动。
- 完成、阻塞、恢复和取消必须保存原因；完成必须保存结果与完成证据。
- `ACT-SPM-01` 等由 Gate 或领域事件驱动的系统活动禁止人工改状态；当前人工命令仅开放给 `ACT-SPM-05/06`。
- 销售项目 Owner 执行本人承担的活动；主管可以追加督办证据，并可取消由主管创建的 `ACT-SPM-06`，但不得代替 Owner 开始或完成。
- 每条命令携带 `commandId + expectedVersion`，用于幂等和乐观并发控制；命令回执账本保存请求指纹，禁止同一 `commandId` 被不同内容或不同操作者复用。
- 失标或终止项目通过 G6 行政关闭时，系统自动取消仍开放的 `ACT-SPM-05/06` 并写入 G6 证据；中标进入 S6 的项目不执行该关闭级联。

## 8. 生命周期活动的系统驱动规则

- G1 批准后，由系统一次性创建并启动 `ACT-SPM-02/03/04`；页面不得自行补造这些活动。
- 策略版本、客户关系记录和角色指派分别把来源对象及证据追加到 `ACT-SPM-02/03/04`。
- G2 批准后完成 `ACT-SPM-02/04`；`ACT-SPM-03` 持续到 G5 商务决策冻结客户关系与决策链输入后完成。
- 正式提交回执入账或 G5 决定不投/不报后创建并启动 `ACT-SPM-07`；结果通知、复盘和 G6 决策持续留证，G6 批准后完成。
- 合同 APP 接收中标基线并推进 S6 时创建并启动 `ACT-SPM-08`。其计划日期是下一次经营检查日期，不等于项目经营关闭日期。
- 生命周期级 `ACT-SPM-02/03/04/07/08` 每个项目每种定义只允许一个实例；重复领域事件不得制造重复活动。

## 6. 禁止事项

- 禁止在销售项目 APP 建设技术、核价、投标、合同人员的完整专业门户。
- 禁止销售员代填技术方案、BOM、成本、底价、专业评审或提交回执。
- 禁止仅凭页面状态推进阶段；必须由 Gate 决策和必要的异步执行回执共同推进。
- 禁止把测试模拟数据伪装成真实外部事实。
