# 部署就绪基线

## 当前结论

当前版本达到“可上传 GitHub、可在本地完成主业务流验证”的标准，但未达到“企业生产系统上线”的标准。Cloudflare Worker/D1/R2 是目标架构，不代表这些能力已经全部实现。

最近一次工程验收：2026-08-25。类型检查、Lint、主系统构建、集成测试台构建及42项单元/契约测试全部通过；隔离D1上的客户直接投标、EPC客户询价、客户直接询价、线索守卫和BidRound治理端到端验收通过。

## 已确认具备

- S0–S6 生命周期与 G1–G7 出口 Gate 代码事实源已统一；G1–G6 可执行，G7 仅提供受控的下游事实跟踪。
- 客户直接投标、EPC 客户询价、客户直接询价使用无歧义场景编码，并持久化 ProcurementRequest、Party 角色和机会指纹。
- EPC 是客户 Party/报价通路，不是经营伙伴；多 EPC 通路不重复创建项目、不叠加项目预计金额。
- 销售 Owner、销售主管、技术/核价/投标/合同模拟责任边界分离，专业结果通过独立 3010 集成测试台回传。
- D1 保存 Gate、版本、活动、风险、外部任务、领域事件和审计记录；集成写入具有来源事件幂等约束。
- GitHub Actions 会执行类型检查、主系统与集成测试台构建、自动化测试和 lint。
- 投标管理可通过独立 `/api/integration/bid/events` 回传BidRound登记和状态事实；S5/S6收到新轮次只进入`on_hold`，不会擅自重启销售项目生命周期。
- 线索自动转化已验证重复线索幂等；金额区间、缺Owner、缺证据和客户主数据未匹配会停在待确认队列。
- `0031_fix_route_result_project_scope.sql` 已把有效通路结果唯一性修正为“项目＋通路”，避免不同直接项目互相占用同一个逻辑通路名。

## 上传 GitHub 前必须通过

1. `pnpm install --frozen-lockfile`
2. `pnpm run typecheck`
3. `pnpm test`
4. `pnpm run lint`
5. 本地分别访问 3000 主系统和 3010 集成测试台，走通至少一条赢单和一条未成交路径。
6. 确认仓库不包含 `.env*`、`.dev.vars`、本地 D1、运行日志、PID、客户真实文件或真实密钥。

## Cloudflare 演示部署前阻断项

- `wrangler.jsonc` 的 D1 `database_id` 仍是本地占位值，必须创建远程 D1 后替换，并在远程执行迁移。
- 3010 使用硬编码的本地演示凭证和固定 CORS 来源，只能在 localhost 使用，禁止作为公网集成鉴权。
- 顶部销售员/主管切换是演示身份，不是真实认证和授权；部署到公网前至少要接入企业 SSO/钉钉身份并由服务端映射角色与数据范围。
- `.openai/hosting.json` 尚未配置 R2；当前仅保存证据引用，未实现文件上传、哈希、访问控制、病毒扫描、保留期和下载审计。
- 尚未建立远程 D1 备份、迁移回滚演练、环境隔离、监控告警和审计导出。
- 真实客户、投标、合同、订单、交付、验收、开票、回款系统的 API 合同、凭证轮换、重试/死信和对账机制尚未确认。

## G7 的受控边界

下游 Contract、Order、Delivery、Acceptance、Invoice、Payment 状态事件可以只读入账，并保留来源系统、来源事件、对象引用、证据与模拟标识。但以下规则没有可信定义：

- 哪些下游状态组合构成经营关闭；
- 应收余额、质保、索赔、未完成行动等阈值；
- 关闭责任人、例外批准人和超期 SLA；
- 已关闭后的重开条件。

因此系统不得提供“人工通过 G7”或“收到一条回款事件即关闭”的按钮。确认企业规则后，应将规则版本化、自动计算准备度，并把例外作为审计事件，而不是页面临时判断。

## 生产上线前新增能力

- 企业认证、最小权限、组织与项目数据范围、离职/调岗回收。
- R2 证据服务和 D1 文件元数据表；前端不得直接信任文件名或用户填入的引用。
- 外部系统连接器与契约测试；本地测试台只保留在非生产环境。
- 数据备份恢复演练、结构化日志、告警、限流、防重放、密钥托管和安全扫描。
- 业务验收用例：正常、退回重提、超时、重复事件、乱序事件、外部系统失败、并发审批、版本失效、失标、终止和赢单移交。
