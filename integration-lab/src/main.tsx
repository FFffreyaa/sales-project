import React, { useEffect, useMemo, useState } from "react";
import { createRoot } from "react-dom/client";
import type { ExternalTaskView, IntegrationEventType } from "../../packages/integration-contracts/src";
import leadSamples from "./fixtures/lead-samples.v1.json";
import "./styles.css";

const API = "http://127.0.0.1:3000/api/integration/lab";
const headers = { "Content-Type": "application/json", "x-integration-key": "qj-local-integration-lab" };
type LabState = { tasks: ExternalTaskView[]; events: Array<Record<string, unknown>>; outbox: Array<Record<string, unknown>>; environment: string; sourceSystem: string };
type LeadSample = (typeof leadSamples)[number];

const scenarioPresets: Record<number, Partial<ReturnType<typeof baseLeadForm>>> = {
  1: { procurementMethod: "tender", requestingPartyRole: "end_customer", requestRef: "TENDER-JS-110KV-2026-01", deliveryLocation: "江苏省", projectCountry: "中国", deliveryCountry: "中国", tenderingEntity: "国网江苏省电力公司", competitorName: "特变电工", partnerName: "" },
  2: { procurementMethod: "direct_rfq", requestingPartyRole: "end_customer", requestRef: "RFQ-GD-220KV-2026-02", deliveryLocation: "广东省", projectCountry: "中国", deliveryCountry: "中国", competitorName: "中国西电", partnerName: "" },
  3: { procurementMethod: "epc_inquiry", requestingPartyRole: "epc", requestRef: "EPC-RFQ-NMG-2026-03", deliveryLocation: "内蒙古自治区", projectCountry: "中国", deliveryCountry: "中国", finalCustomerName: "内蒙古农网建设业主单位", requestingPartyName: "内蒙古电力设计院EPC项目部", submissionRecipientName: "内蒙古电力设计院EPC项目部", designInstitute: "内蒙古电力设计院", competitorName: "山东电工", partnerName: "" },
  4: { procurementMethod: "epc_inquiry", requestingPartyRole: "epc", requestRef: "OS-EPC-RFQ-2026-04", deliveryLocation: "雅加达项目现场", projectCountry: "印度尼西亚", deliveryCountry: "印度尼西亚", finalCustomerName: "印尼国家电力项目业主", requestingPartyName: "中国能建海外EPC项目部", submissionRecipientName: "中国能建海外EPC项目部", designInstitute: "中国能建海外设计中心", competitorName: "国际变压器厂商A", partnerName: "印尼属地服务伙伴候选" },
};

function baseLeadForm(sample: LeadSample) {
  return {
    sampleId: String(sample.id), leadCategory: sample.leadCategory, grade: "A", projectName: sample.projectName, customerName: sample.clientUnit,
    finalCustomerName: sample.clientUnit, reporter: sample.reporter, sourceChannel: sample.channel, description: sample.description,
    amountWan: String(sample.estimatedAmountWan), procurementMethod: "unknown", suggestedScenarioCode: "", requestingPartyName: sample.clientUnit, requestingPartyRole: "unknown",
    submissionRecipientName: sample.clientUnit, requestRef: "", requestDate: "2026-08-25", deadline: "2026-09-30", deliveryLocation: "",
    projectCountry: "中国", deliveryCountry: "中国", tenderingEntity: "", designInstitute: "", installationContractor: "", customerCode: `TEMP-CUSTOMER-${sample.id}`,
    quantity: "", competitorName: "特变电工", keyRoleName: "张总监", keyRolePosition: "采购总监", partnerName: "", evidenceRef: `LEAD-SAMPLE-${sample.id}`,
  };
}

const leadFormFrom = (sample: LeadSample) => ({ ...baseLeadForm(sample), ...(scenarioPresets[Number(sample.id)] ?? {}), suggestedScenarioCode: (["", "SCN-01-DIRECT-BID", "SCN-03-DIRECT-RFQ", "SCN-02-EPC-INQUIRY", "SCN-04-OVERSEAS-PARTNER-EPC"] as const)[Number(sample.id)] ?? "" });

const taskLabels: Record<string, string> = { BID_PREPARATION: "投标专员｜采购请求拆解与投标计划", COMMERCIAL_SOLUTION: "商务经理｜商务方案与偏差", TECHNICAL_COLLABORATION: "技术负责人｜S1需求与方案启动评估", REQUIREMENT_BASELINE_PREPARATION: "需求基线责任｜正式需求基线", TECHNICAL_SOLUTION_PREPARATION: "技术责任｜技术方案", BOM_AND_CLOSURE_PREPARATION: "方案设计责任｜报价设计BOM与问题闭环", G3_GATE_REVIEW: "技术评审人｜G3基线评审", COSTING_COLLABORATION: "S3核价协同任务", G4_GATE_REVIEW: "G4核价与价格授权确认", EPC_PRICE_EXCEPTION_APPROVAL: "EPC通路价格例外审批", BID_PACKAGE_PREPARATION: "投标专员｜S4提交包编制", BID_PACKAGE_REVIEW: "专业评审人｜投标包评审/复核", BID_REVIEW_REMEDIATION: "投标专员｜专业评审问题整改", G5_SUBMISSION_EXECUTION: "投标专员｜G5正式提交", COMMERCIAL_RESULT_TRACKING: "L2.9商业结果跟踪/核实", CONTRACT_HANDOVER_EXECUTION: "G6中标基线合同接收", DOWNSTREAM_STATUS_TRACKING: "G7下游只读状态跟踪" };
const statusLabels: Record<string, string> = { pending: "待接受/待处理", accepted: "处理中", completed: "已完成", returned: "已退回", rejected: "失败", cancelled: "已取消" };

function objectOf(value: unknown) {
  if (value && typeof value === "object") return value as Record<string, unknown>;
  if (typeof value !== "string") return {};
  try { return JSON.parse(value) as Record<string, unknown>; } catch { return {}; }
}

function App() {
  const [state, setState] = useState<LabState>({ tasks: [], events: [], outbox: [], environment: "demo", sourceSystem: "TECHNICAL_COLLABORATION_SIMULATOR" });
  const [selectedId, setSelectedId] = useState("");
  const [message, setMessage] = useState("正在连接销售项目系统……");
  const [busy, setBusy] = useState(false);
  const [leadForm, setLeadForm] = useState(() => leadFormFrom(leadSamples[0]));
  const [solution, setSolution] = useState({ requirementGaps: "客户技术参数需进一步核对", technicalDifficulty: "关键参数与交期存在联合约束", solutionResourceNeeds: "需要技术负责人和方案工程师协同", evidenceRef: "LAB-S1-PRECHECK-001" });
  const [bidPreparation, setBidPreparation] = useState({ sourceType: "TenderRequest", sourceRef: "", sourceVersion: "V1", sourceHash: "SHA256:LAB-SOURCE-V1", technicalRequirements: "已提取技术规范、功能与性能边界，详细参数进入后续需求基线作业。", commercialRequirements: "已提取付款、交付、保证金和违约责任等商务条款。", evaluationCriteria: "已识别评标/评审方法、分值与否决项。", qualificationRequirements: "已识别资质、业绩、授权及有效期要求。", identifiedRisks: "交期与关键资料存在联动风险，已纳入工作计划。", bidStrategy: "围绕技术符合性、交付可信度和全生命周期价值形成投标响应策略。", evidenceRef: "LAB-BID-PREPARATION-001", workPlan: [{ task: "完成需求与技术方案基线", ownerRole: "技术负责人", dueDate: "2026-09-06" }, { task: "完成商务条款响应与偏差评估", ownerRole: "商务经理", dueDate: "2026-09-07" }, { task: "完成提交包组装与校验", ownerRole: "投标专员", dueDate: "2026-09-10" }] });
  const [commercialSolution, setCommercialSolution] = useState({ bidPreparationVersionId: "", paymentTerms: "按采购请求逐条响应付款节点，偏差见商务偏差清单。", deliveryTerms: "按项目交付窗口形成响应，关键节点与投标工作计划一致。", guaranteeTerms: "保证金与质保责任按采购请求响应，例外项单独授权。", breachLiability: "违约责任已逐条识别并形成风险分级。", pricingStrategy: "报价策略引用后续核价和价格授权，本版本不擅自确定最终价格。", quotationListRef: "QUOTE-LIST-DRAFT-001", riskAssessment: "商务条款总体可响应，高风险偏差必须在最终提交前完成授权。", mitigationPlan: "由商务经理跟踪偏差关闭，投标专员在组包时校验授权引用。", evidenceRef: "LAB-COMMERCIAL-SOLUTION-001", commercialDeviations: [{ clause: "付款与交付联动条款", response: "原则响应；关键验收节点需澄清", riskLevel: "medium", mitigation: "在正式提交前取得客户澄清或内部授权" }] });
  const [technical, setTechnical] = useState({ standardizedRequirement: "按客户原始需求及澄清结果形成的正式技术需求", requirementEvidenceRef: "LAB-S2-REQUIREMENT-BASELINE-001", designAdoptedValue: "采用值与当前需求基线一致", criticalParameterStatus: "完整", initialTechnicalSolution: "形成满足当前需求边界的初步技术方案", solutionEvidenceRef: "LAB-S2-SOLUTION-001", configurationSummary: "已按技术方案形成报价设计BOM，数量、范围与配置逐项对应", bomConfirmationStatus: "confirmed", bomEvidenceRef: "LAB-S2-BOM-CONFIRM-001", clarificationQuestion: "当前无未决技术澄清", clarificationResponse: "已根据客户需求来源完成核对", clarificationStatus: "resolved", clarificationOwnerRole: "需求/技术问题责任人", clarificationClosureEvidenceRef: "LAB-S2-CLARIFICATION-CLOSE-001", deviationDescription: "当前无技术偏差", deviationStatus: "none", deviationEvidenceRef: "LAB-S2-DEVIATION-CHECK-001", deviationOwnerRole: "方案设计责任人", deviationDisposition: "经逐项核对，当前版本无技术偏差", deviationClosureEvidenceRef: "LAB-S2-DEVIATION-CLOSE-001" });
  const [costing, setCosting] = useState({ priceSource: "采购询价、现行材料价目与制造费用标准", priceSourceVersion: "2026-08-V1", snapshotDate: "2026-08-20", validUntil: "2026-09-20", currency: "CNY", taxBasis: "含13%增值税", tradeTerms: "境内含税交付至项目现场", exchangeRateBasis: "不适用（人民币）", priceEvidenceRef: "LAB-S3-PRICE-SNAPSHOT-001", materialCostYuan: "1800000", laborCostYuan: "120000", manufacturingCostYuan: "260000", transportCostYuan: "80000", taxCostYuan: "210000", riskReserveYuan: "130000", costingSalesPriceYuan: "3200000", targetProfitRate: "12", calculationBasis: "引用已批准技术方案和报价设计BOM，按完整成本、风险储备与目标利润率测算；最终外部报价与授权底价另行决策", deliveryAssessment: "批准技术配置在当前产能与采购周期下可按计划交付", deliveryRiskConclusion: "交期风险可控，关键材料有效期内下单" });
  const [decision, setDecision] = useState({ action: "approve", comment: "需求、方案、关键参数、澄清与偏差记录已核对。" });
  const [exceptionDecision, setExceptionDecision] = useState({ action: "approve", comment: "已核对通路差异原因、证据、底价与授权有效期，同意本通路例外报价。" });
  const [authorization, setAuthorization] = useState({ floorPriceYuan: "3000000", authorizedQuotePriceYuan: "3200000", currency: "CNY", taxBasis: "含13%增值税", tradeTerms: "境内含税交付至项目现场", routeScope: "DIRECT", exceptionConditions: "范围、币种、税费、贸易条件或有效期变化时本授权失效并重新申请", scopeAlignmentConclusion: "已核对项目机会范围、当前需求基线和本次报价范围一致", amountVarianceExplanation: "项目预计金额是销售预测口径，本次授权报价以当前需求、数量、配置和通路范围为准；差异已复核并留痕", validUntil: "2026-09-20", evidenceRef: "LAB-G4-PRICE-AUTH-001" });
  const [bidPackage, setBidPackage] = useState({ submissionType: "DirectBidSubmission", routeId: "DIRECT", quotedPriceYuan: "3200000", packageHash: "SHA256:LAB-BID-PACKAGE-001", evidenceRef: "LAB-S4-BID-PACKAGE-001" });
  const [professionalReview, setProfessionalReview] = useState({ reviewType: "business", reviewConclusion: "approved", openRiskCount: "0", deviationConclusion: "none", reviewSummary: "已按指定专业维度核对投标包，未发现阻断提交的问题。", evidenceRef: "LAB-S4-PROFESSIONAL-REVIEW-001", remediationId: "" });
  const [reviewRemediation, setReviewRemediation] = useState({ issueResponse: "已逐项完成专业评审问题整改，并更新相应投标包支撑证据。", evidenceRef: "LAB-S4-REVIEW-REMEDIATION-001" });
  const [receipt, setReceipt] = useState({ submittedAt: new Date().toISOString().slice(0, 16), receiptRef: "PLATFORM-RCPT-001", receiptHash: "SHA256:LAB-RECEIPT-001" });
  const [commercialResult, setCommercialResult] = useState({ resultType: "won", sourceRef: "AWARD-NOTICE-001", noticeDate: new Date().toISOString().slice(0, 10), evidenceRef: "LAB-L29-RESULT-EVIDENCE-001", awardScope: "客户正式通知的中标产品、数量与承诺范围", competitorName: "未披露", correctionReason: "已复核正式结果来源，原通路结果登记有误" });
  const [contractReceipt, setContractReceipt] = useState({ contractRef: "CONTRACT-DRAFT-001", receiptRef: "CONTRACT-HANDOVER-RCPT-001", receiptHash: "SHA256:CONTRACT-HANDOVER-001", receivedAt: new Date().toISOString().slice(0, 16) });
  const [contractReturn, setContractReturn] = useState({ differenceSummary: "合同草案中的交付或责任条款与中标承诺基线不一致", remediationRequirement: "请定位差异来源并形成新的受控基线版本后重新移交", evidenceRef: "LAB-CONTRACT-DIFFERENCE-001" });
  const [downstream, setDownstream] = useState({ eventType: "ContractStatusReported", objectRef: "CONTRACT-001", businessStatus: "合同已生效", amountYuan: "", currency: "CNY", evidenceRef: "LAB-DOWNSTREAM-STATUS-001" });

  const load = async () => {
    try {
      const response = await fetch(`${API}/tasks`, { headers });
      const data = await response.json() as LabState & { error?: string };
      if (!response.ok) throw new Error(data.error ?? "读取任务失败");
      setState(data);
      setSelectedId(current => current && data.tasks.some(task => task.external_task_id === current) ? current : data.tasks[0]?.external_task_id ?? "");
      setMessage(`已连接｜${new Date().toLocaleTimeString("zh-CN")}`);
    } catch (error) { setMessage(error instanceof Error ? error.message : "无法连接3000端口"); }
  };
  useEffect(() => { const timer = window.setTimeout(() => void load(), 0); return () => window.clearTimeout(timer); }, []);
  const selected = useMemo(() => state.tasks.find(task => task.external_task_id === selectedId), [state.tasks, selectedId]);

  const send = async (eventType: IntegrationEventType, data: Record<string, unknown>, evidenceRefs: string[] = []) => {
    if (!selected) return;
    setBusy(true);
    try {
      const bidSystem = selected.task_type === "BID_PREPARATION" || selected.task_type === "COMMERCIAL_SOLUTION" || selected.task_type === "BID_PACKAGE_PREPARATION" || selected.task_type === "BID_PACKAGE_REVIEW" || selected.task_type === "BID_REVIEW_REMEDIATION" || selected.task_type === "G5_SUBMISSION_EXECUTION" || selected.task_type === "COMMERCIAL_RESULT_TRACKING";
      const costingSystem = selected.task_type === "COSTING_COLLABORATION" || selected.task_type === "G4_GATE_REVIEW" || selected.task_type === "EPC_PRICE_EXCEPTION_APPROVAL";
      const contractSystem = selected.task_type === "CONTRACT_HANDOVER_EXECUTION" || selected.task_type === "DOWNSTREAM_STATUS_TRACKING";
      const actorId = contractSystem ? "contract-admin-liu" : selected.task_type === "COMMERCIAL_SOLUTION" ? "commercial-manager-tang" : selected.task_type === "BID_PACKAGE_REVIEW" ? selected.assignee_external_id : bidSystem ? "bid-specialist-sun" : selected.task_type === "REQUIREMENT_BASELINE_PREPARATION" ? "requirement-owner-demo" : selected.task_type === "BOM_AND_CLOSURE_PREPARATION" ? "solution-designer-demo" : selected.task_type === "G3_GATE_REVIEW" ? "technical-reviewer-wang" : selected.task_type === "COSTING_COLLABORATION" ? "costing-liu" : selected.task_type === "G4_GATE_REVIEW" || selected.task_type === "EPC_PRICE_EXCEPTION_APPROVAL" ? "authorizer-luo" : "technical-zhao";
      const envelope = { eventId: crypto.randomUUID(), eventType, sourceSystem: contractSystem ? "CONTRACT_COLLABORATION_SIMULATOR" : bidSystem ? "BID_COLLABORATION_SIMULATOR" : costingSystem ? "COSTING_COLLABORATION_SIMULATOR" : "TECHNICAL_COLLABORATION_SIMULATOR", environment: "demo", simulated: true, projectCode: selected.project_code, externalTaskId: selected.external_task_id, actorId, occurredAt: new Date().toISOString(), data, evidenceRefs };
      const response = await fetch(`${API}/events`, { method: "POST", headers, body: JSON.stringify(envelope) });
      const result = await response.json() as { error?: string; status?: string };
      if (!response.ok) throw new Error(result.error ?? "事件回传失败");
      setMessage(`${eventType} 已处理并留痕`);
      await load();
    } catch (error) {
      const failure = error instanceof Error ? error.message : "事件回传失败";
      await load();
      setMessage(`处理失败｜${failure}`);
    }
    finally { setBusy(false); }
  };

  const sendLead = async () => {
    setBusy(true);
    try {
      if (!leadForm.suggestedScenarioCode) throw new Error("请先选择线索APP维护的业务场景");
      const stamp = Date.now();
      const tender = leadForm.procurementMethod === "tender";
      const amountYuan = Number(leadForm.amountWan) * 10_000;
      const envelope = {
        eventId: `LEAD-EVENT-${stamp}`, eventType: "LeadQualifiedForConversion", sourceSystem: "LEAD_MANAGEMENT_SIMULATOR", environment: "demo", simulated: true, occurredAt: new Date().toISOString(),
        lead: { id: `LEAD-${stamp}`, code: `LEAD-20260825-${String(stamp).slice(-4)}`, type: tender ? "tender" : "demand", category: leadForm.leadCategory, grade: leadForm.grade, score: 82, authenticityStatus: "verified", authenticityBasis: "线索真实性审核已通过；本地测试样本不代表生产结论", sourceChannel: leadForm.sourceChannel, assignedOwnerExternalId: "sales-chen", assignedOwnerName: "陈晨", submitterExternalId: `lead-reporter-${leadForm.sampleId}`, submitterName: leadForm.reporter, submittedAt: new Date().toISOString(), gradeAssessment: { dimensions: { projectScale: leadForm.grade, industryValue: "A", customerValue: "A", regionValue: "A", productValue: "A" }, basis: "线索侧五维评估；销售项目G1仅把它作为等级建议", ruleVersion: "LEAD-GRADE-DEMO-V0.3", manuallyAdjusted: false }, majorProject: { flagged: leadForm.grade === "S", basis: "模拟样本标记，企业阈值待确认", source: "manual" } },
        opportunity: { projectName: leadForm.projectName, customerName: leadForm.customerName, finalCustomerName: leadForm.finalCustomerName || undefined, projectCountry: leadForm.projectCountry, deliveryCountry: leadForm.deliveryCountry, productScope: leadForm.description, quantity: leadForm.quantity, amount: { type: Number.isFinite(amountYuan) && amountYuan > 0 ? "exact" : "unknown", minYuan: Number.isFinite(amountYuan) && amountYuan > 0 ? amountYuan : undefined, maxYuan: Number.isFinite(amountYuan) && amountYuan > 0 ? amountYuan : undefined, currency: "CNY" }, procurementProgress: leadForm.procurementMethod === "unknown" ? "待销售确认" : leadForm.procurementMethod, procurementMethod: leadForm.procurementMethod, requestingPartyName: leadForm.requestingPartyName, requestingPartyRole: leadForm.requestingPartyRole, submissionRecipientName: leadForm.submissionRecipientName, suggestedScenarioCode: leadForm.suggestedScenarioCode || undefined, requestRef: leadForm.requestRef || undefined, requestDate: leadForm.requestRef ? leadForm.requestDate : undefined, deliveryLocation: leadForm.deliveryLocation, submissionDeadline: leadForm.deadline, competitorContext: leadForm.competitorName || "暂无已识别竞对", fundingSource: "待销售确认", sourcePlatform: "老师提供的线索管理HTML样本", customerMaster: { code: leadForm.customerCode, matchStatus: "temporary" } },
        initialRequirement: { originalText: leadForm.description, productRequirement: leadForm.description, quantity: leadForm.quantity || undefined, knownConstraints: [leadForm.deliveryLocation ? `交付地点：${leadForm.deliveryLocation}` : "", leadForm.deadline ? `客户要求时间：${leadForm.deadline}` : ""].filter(Boolean), unknowns: [!leadForm.quantity ? "数量待澄清" : "", !leadForm.requestRef ? "客户采购请求编号待确认" : ""].filter(Boolean), sourceRefs: [leadForm.evidenceRef] },
        evidenceRefs: [leadForm.evidenceRef], competitors: leadForm.competitorName ? [{ name: leadForm.competitorName, source: leadForm.evidenceRef, confidence: "medium", verification: "线索侧记录，待项目Owner确认" }] : [], keyRoles: leadForm.keyRoleName ? [{ name: leadForm.keyRoleName, position: leadForm.keyRolePosition, role: "决策者候选", attitude: "中立", concern: "价格与交付", evidence: leadForm.evidenceRef }] : [], partners: leadForm.partnerName ? [{ name: leadForm.partnerName, type: "项目协作伙伴候选", status: "线索侧候选", evidence: leadForm.evidenceRef }] : [],
        contacts: leadForm.keyRoleName ? [{ name: leadForm.keyRoleName, position: leadForm.keyRolePosition, verification: "线索侧记录，待销售确认" }] : [], followups: [{ occurredAt: "2026-08-24", actor: leadForm.reporter, summary: "线索跟进事实与业务场景已形成，等待销售项目人工转化" }], attachments: [{ name: "线索来源证据", type: tender ? "tender_notice" : "lead_evidence", source: "LEAD_MANAGEMENT_SIMULATOR", ref: leadForm.evidenceRef }],
        parties: [
          { externalPartyId: `CUSTOMER-${leadForm.sampleId}`, masterCode: leadForm.customerCode, name: leadForm.customerName, role: leadForm.requestingPartyRole === "epc" ? "epc" : "requesting_customer", country: leadForm.projectCountry, masterStatus: "temporary", evidence: leadForm.evidenceRef },
          ...(leadForm.finalCustomerName ? [{ externalPartyId: `OWNER-${leadForm.sampleId}`, name: leadForm.finalCustomerName, role: "end_customer", country: leadForm.projectCountry, masterStatus: "temporary", evidence: leadForm.evidenceRef }] : []),
          ...(leadForm.tenderingEntity ? [{ name: leadForm.tenderingEntity, role: "tendering_entity", country: leadForm.projectCountry, masterStatus: "temporary", evidence: leadForm.evidenceRef }] : []),
          ...(leadForm.designInstitute ? [{ name: leadForm.designInstitute, role: "design_institute", country: leadForm.projectCountry, masterStatus: "temporary", evidence: leadForm.evidenceRef }] : []),
          ...(leadForm.installationContractor ? [{ name: leadForm.installationContractor, role: "installation_contractor", country: leadForm.deliveryCountry, masterStatus: "temporary", evidence: leadForm.evidenceRef }] : []),
          ...(leadForm.partnerName ? [{ name: leadForm.partnerName, role: "partner", country: leadForm.deliveryCountry, masterStatus: "unmatched", evidence: leadForm.evidenceRef }] : []),
        ],
        duplicateCheck: { status: "clear", checkedAt: new Date().toISOString(), basis: "线索侧查重结果只作为来源事实；销售项目仍按采购机会指纹执行幂等校验" },
        fieldProvenance: { fixtureId: `HTML-LEAD-${leadForm.sampleId}`, projectName: "线索管理HTML样本", customerName: "线索管理HTML样本", productScope: "线索描述", amount: "线索预估金额", requestRef: leadForm.requestRef ? "测试人员补充" : "未形成", scenarioCode: "线索APP维护" },
      };
      const response = await fetch("http://127.0.0.1:3000/api/integration/lead/events", { method: "POST", headers, body: JSON.stringify(envelope) });
      const result = await response.json() as { error?: string; autoConverted?: boolean; project?: { projectCode?: string } };
      if (!response.ok) throw new Error(result.error ?? "线索事件发送失败");
      setMessage(result.autoConverted ? "异常：线索不应自动形成项目" : "线索及业务场景已进入3000待转化队列，需人工核对后生成S0/G1");
    } catch (error) { setMessage(error instanceof Error ? error.message : "线索事件发送失败"); }
    finally { setBusy(false); }
  };

  const field = (label: string, value: string, onChange: (value: string) => void) => <label><span>{label}</span><textarea value={value} onChange={event => onChange(event.target.value)} /></label>;
  const payload = selected ? objectOf(selected.request_payload) : {};
  const reviewId = String(selected?.review_id ?? payload.gateId ?? payload.reviewId ?? "");
  const pendingExitForProject = selected ? state.tasks.find(task => task.project_code === selected.project_code && task.task_type === "G3_GATE_REVIEW" && task.status === "pending") : undefined;
  const technicalSnapshotLocked = Boolean(selected && ["REQUIREMENT_BASELINE_PREPARATION", "TECHNICAL_SOLUTION_PREPARATION", "BOM_AND_CLOSURE_PREPARATION"].includes(selected.task_type) && pendingExitForProject);
  const reviewSnapshot = selected ? objectOf(selected.review_snapshot) : {};
  const reviewItems = Array.isArray(reviewSnapshot.items) ? reviewSnapshot.items as Array<Record<string, unknown>> : [];
  const reviewBlockers = reviewItems.filter(item => item.hard === true && item.ready !== true);
  const selectTask = (task: ExternalTaskView) => {
    setSelectedId(task.external_task_id);
    if (task.task_type === "BID_PREPARATION") {
      const taskPayload = objectOf(task.request_payload);
      setBidPreparation(current => ({ ...current, sourceType: String(taskPayload.sourceType ?? current.sourceType), sourceRef: String(taskPayload.sourceRef ?? current.sourceRef) }));
    }
    if (task.task_type === "COMMERCIAL_SOLUTION") setCommercialSolution(current => ({ ...current, bidPreparationVersionId: String(objectOf(task.request_payload).bidPreparationVersionId ?? current.bidPreparationVersionId) }));
    if (task.task_type === "G4_GATE_REVIEW") setAuthorization(current => ({ ...current, routeScope: ["SCN-02-EPC-INQUIRY", "SCN-04-OVERSEAS-PARTNER-EPC"].includes(String(objectOf(task.request_payload).scenarioCode)) ? "ALL-EPC-ROUTES" : current.routeScope }));
    if (task.task_type === "BID_PACKAGE_PREPARATION") {
      const routeId = String(objectOf(task.request_payload).routeId ?? objectOf(task.request_payload).routeScope ?? "DIRECT");
      setBidPackage(current => ({ ...current, submissionType: String(objectOf(task.request_payload).expectedSubmissionType ?? current.submissionType), routeId: routeId.split(/[，,;；\s]+/)[0], quotedPriceYuan: String(objectOf(task.request_payload).authorizedQuotePriceYuan ?? current.quotedPriceYuan), packageHash: `SHA256:${task.external_task_id}-${routeId.slice(-8)}` }));
    }
    if (task.task_type === "BID_PACKAGE_REVIEW") {
      const taskPayload = objectOf(task.request_payload);
      const reviewType = String(taskPayload.reviewType ?? "business");
      const reviewLabel = String(taskPayload.reviewLabel ?? "专业评审");
      setProfessionalReview(current => ({ ...current, reviewType, remediationId: String(taskPayload.remediationId ?? ""), reviewConclusion: "approved", openRiskCount: "0", reviewSummary: taskPayload.remediationId ? `已复核${reviewLabel}整改证据，原问题已关闭。` : `已完成${reviewLabel}，未发现阻断提交的问题。`, evidenceRef: `LAB-S4-${reviewType.toUpperCase()}-REVIEW-${task.external_task_id}` }));
    }
    if (task.task_type === "G5_SUBMISSION_EXECUTION") setReceipt(current => ({ ...current, receiptRef: `RCPT-${task.external_task_id}`, receiptHash: `SHA256:RCPT-${task.external_task_id}` }));
    if (task.task_type === "COMMERCIAL_RESULT_TRACKING") {
      const taskPayload = objectOf(task.request_payload);
      const routeId = String(taskPayload.routeId ?? "DIRECT");
      setCommercialResult(current => ({ ...current, resultType: taskPayload.mode === "correction" && taskPayload.priorResultType === "won" ? "lost" : current.resultType, sourceRef: `RESULT-${task.external_task_id}`, evidenceRef: `EVIDENCE-${task.external_task_id}`, awardScope: `${routeId} 对应客户正式通知的中标范围`, correctionReason: String(taskPayload.correctionRequestReason ?? current.correctionReason) }));
    }
    if (!["G3_GATE_REVIEW", "G4_GATE_REVIEW"].includes(task.task_type) || task.status !== "pending") return;
    const snapshot = objectOf(task.review_snapshot);
    const items = Array.isArray(snapshot.items) ? snapshot.items as Array<Record<string, unknown>> : [];
    const blockers = items.filter(item => item.hard === true && item.ready !== true);
    setDecision(blockers.length
      ? { action: "return", comment: `硬条件未齐套：${blockers.map(item => String(item.label)).join("、")}。请先更新来源版本后重新提交。` }
      : { action: "approve", comment: task.task_type === "G4_GATE_REVIEW" ? "报价设计BOM、价格快照、完整成本、利润测算和交期风险结论已核对。" : "需求、方案、关键参数、澄清与偏差记录已核对。" });
  };

  return <div className="shell">
    <header><div><span>LOCAL INTEGRATION LAB</span><h1>钱江电气｜外部系统集成测试台</h1><p>集中模拟技术、核价、财务授权、投标作业与合同系统的任务接受、专业结果和受控回执。</p></div><button onClick={() => void load()}>刷新任务</button></header>
    <div className="warning"><strong>仅本地测试，不是业务系统，也不是权威数据来源</strong><span>这里的操作均标记 environment=demo、simulated=true；3000 主系统只展示结果，不允许销售角色代做专业结论。</span></div>
    <div className="connection"><i />{message}<code>主系统 127.0.0.1:3000</code><code>测试台 127.0.0.1:3010</code></div>
    <section className="lead-simulator lead-detailed"><div className="lead-title"><span>LEAD MANAGEMENT SIMULATOR</span><h2>线索管理｜待转化事件模拟</h2><p>12条样本来自老师提供的线索HTML。业务场景由线索APP维护并随事件传入；销售项目只核对，不重新推测，也不会自动创建S0。</p></div><label><span>HTML样本</span><select value={leadForm.sampleId} onChange={event => { const sample = leadSamples.find(item => String(item.id) === event.target.value); if (sample) setLeadForm(leadFormFrom(sample)); }}>{leadSamples.map(item => <option key={item.id} value={item.id}>{item.id}. {item.projectName}</option>)}</select></label><label><span>业务场景（线索APP维护）</span><select value={leadForm.suggestedScenarioCode} onChange={event => setLeadForm(current => ({ ...current, suggestedScenarioCode: event.target.value as typeof current.suggestedScenarioCode }))}><option value="">请选择业务场景</option><option value="SCN-01-DIRECT-BID">国内直接投标</option><option value="SCN-02-EPC-INQUIRY">国内EPC询价</option><option value="SCN-03-DIRECT-RFQ">国内客户直接询价</option><option value="SCN-04-OVERSEAS-PARTNER-EPC">海外伙伴/EPC询价</option></select></label><button disabled={busy || !leadForm.suggestedScenarioCode} onClick={() => void sendLead()}>发送到待转化队列</button><details><summary>编辑本次模拟线索的详细事实</summary><div className="lead-form-grid">
      <label><span>线索分类</span><select value={leadForm.leadCategory} onChange={event => setLeadForm(current => ({ ...current, leadCategory: event.target.value }))}><option>客户线索</option><option>项目线索</option><option>伙伴线索</option></select></label>
      <label><span>线索等级建议</span><select value={leadForm.grade} onChange={event => setLeadForm(current => ({ ...current, grade: event.target.value }))}><option>S</option><option>A</option><option>B</option><option>C</option></select></label>
      <label><span>项目名称</span><input value={leadForm.projectName} onChange={event => setLeadForm(current => ({ ...current, projectName: event.target.value }))} /></label>
      <label><span>线索客户</span><input value={leadForm.customerName} onChange={event => setLeadForm(current => ({ ...current, customerName: event.target.value }))} /></label>
      <label><span>最终业主（可空）</span><input value={leadForm.finalCustomerName} onChange={event => setLeadForm(current => ({ ...current, finalCustomerName: event.target.value }))} /></label>
      <label><span>项目所在国家</span><input value={leadForm.projectCountry} onChange={event => setLeadForm(current => ({ ...current, projectCountry: event.target.value }))} /></label>
      <label><span>交付国家</span><input value={leadForm.deliveryCountry} onChange={event => setLeadForm(current => ({ ...current, deliveryCountry: event.target.value }))} /></label>
      <label><span>客户主数据编码</span><input value={leadForm.customerCode} onChange={event => setLeadForm(current => ({ ...current, customerCode: event.target.value }))} /></label>
      <label><span>预计金额（万元）</span><input type="number" value={leadForm.amountWan} onChange={event => setLeadForm(current => ({ ...current, amountWan: event.target.value }))} /></label>
      <label><span>采购信号</span><select value={leadForm.procurementMethod} onChange={event => setLeadForm(current => ({ ...current, procurementMethod: event.target.value }))}><option value="unknown">未知/待确认</option><option value="tender">正式招标信号</option><option value="epc_inquiry">EPC询价信号</option><option value="direct_rfq">客户直接询价信号</option></select></label>
      <label><span>请求方</span><input value={leadForm.requestingPartyName} onChange={event => setLeadForm(current => ({ ...current, requestingPartyName: event.target.value }))} /></label>
      <label><span>请求方角色</span><select value={leadForm.requestingPartyRole} onChange={event => setLeadForm(current => ({ ...current, requestingPartyRole: event.target.value }))}><option value="unknown">未知</option><option value="end_customer">最终客户</option><option value="epc">EPC</option><option value="tender_agent">招标代理</option></select></label>
      <label><span>钱江提交对象</span><input value={leadForm.submissionRecipientName} onChange={event => setLeadForm(current => ({ ...current, submissionRecipientName: event.target.value }))} /></label>
      <label><span>招标主体（可空）</span><input value={leadForm.tenderingEntity} onChange={event => setLeadForm(current => ({ ...current, tenderingEntity: event.target.value }))} /></label>
      <label><span>设计院（可空）</span><input value={leadForm.designInstitute} onChange={event => setLeadForm(current => ({ ...current, designInstitute: event.target.value }))} /></label>
      <label><span>安装单位（可空）</span><input value={leadForm.installationContractor} onChange={event => setLeadForm(current => ({ ...current, installationContractor: event.target.value }))} /></label>
      <label><span>采购请求编号（可空）</span><input value={leadForm.requestRef} onChange={event => setLeadForm(current => ({ ...current, requestRef: event.target.value }))} /></label>
      <label><span>请求日期</span><input type="date" value={leadForm.requestDate} onChange={event => setLeadForm(current => ({ ...current, requestDate: event.target.value }))} /></label>
      <label><span>提交截止日期</span><input type="date" value={leadForm.deadline} onChange={event => setLeadForm(current => ({ ...current, deadline: event.target.value }))} /></label>
      <label><span>竞争对手候选</span><input value={leadForm.competitorName} onChange={event => setLeadForm(current => ({ ...current, competitorName: event.target.value }))} /></label>
      <label><span>关键角色候选</span><input value={leadForm.keyRoleName} onChange={event => setLeadForm(current => ({ ...current, keyRoleName: event.target.value }))} /></label>
      <label><span>关键角色职位</span><input value={leadForm.keyRolePosition} onChange={event => setLeadForm(current => ({ ...current, keyRolePosition: event.target.value }))} /></label>
      <label><span>合作伙伴候选（可空）</span><input value={leadForm.partnerName} onChange={event => setLeadForm(current => ({ ...current, partnerName: event.target.value }))} /></label>
      <label><span>证据引用</span><input value={leadForm.evidenceRef} onChange={event => setLeadForm(current => ({ ...current, evidenceRef: event.target.value }))} /></label>
      <label className="wide"><span>产品需求/线索描述</span><textarea value={leadForm.description} onChange={event => setLeadForm(current => ({ ...current, description: event.target.value }))} /></label>
    </div></details></section>
    <main>
      <aside><h2>外部任务</h2>{state.tasks.length ? state.tasks.map(task => <button className={task.external_task_id === selectedId ? "selected" : ""} key={task.external_task_id} onClick={() => selectTask(task)}><span>{taskLabels[task.task_type]}</span><strong>{task.project_code}</strong><small>{task.project_name}</small><em>{statusLabels[task.status] ?? task.status}</em></button>) : <div className="empty">暂无外部任务。请先在3000推进相应业务阶段。</div>}</aside>
      <section className="workspace">
        {!selected ? <div className="empty large">选择任务后处理</div> : <>
          <div className="task-head"><div><span>{taskLabels[selected.task_type]} · {selected.external_task_id}</span><h2>{selected.project_name}</h2><p>{selected.project_code}｜{selected.stage} {selected.stage_name}｜责任人 {selected.assignee_name}</p></div><b>{statusLabels[selected.status] ?? selected.status}</b></div>
          {selected.task_type === "EPC_PRICE_EXCEPTION_APPROVAL" && selected.status === "pending" && <article><h3>EPC通路价格例外审批</h3><p>项目统一授权报价 ¥{Number(payload.baseAuthorizedPriceYuan ?? 0).toLocaleString("zh-CN")}；通路申请报价 ¥{Number(payload.requestedPriceYuan ?? 0).toLocaleString("zh-CN")}。例外只作用于“{String(payload.epcCustomer ?? payload.routeId ?? "该通路")}”，不得覆盖项目统一报价，也不得低于授权底价 ¥{Number(payload.floorPriceYuan ?? 0).toLocaleString("zh-CN")}。</p><div className="form"><label><span>授权结论</span><select value={exceptionDecision.action} onChange={event => setExceptionDecision(current => ({ ...current, action: event.target.value }))}><option value="approve">批准通路例外</option><option value="return">退回补充</option><option value="reject">拒绝例外</option></select></label>{field("审批意见", exceptionDecision.comment, value => setExceptionDecision(current => ({ ...current, comment: value })))}</div><button disabled={busy || !payload.exceptionId} onClick={() => void send("EpcPriceExceptionDecisionSubmitted", { exceptionId: String(payload.exceptionId), action: exceptionDecision.action, comment: exceptionDecision.comment }, [String(payload.evidenceRef ?? "")])}>提交价格授权结论</button></article>}
          {selected.task_type === "BID_PREPARATION" && selected.status === "pending" && <article><h3>1. 接受投标准备任务</h3><p>投标专员负责拆解已登记的招标/询价来源，形成投标策略和工作计划；销售项目赢单策略仍由销售Owner维护。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "孙投标已接受采购请求拆解与投标准备责任" }, ["LAB-BID-PREPARATION-ACCEPTANCE"])}>孙投标接受投标准备任务</button></article>}
          {selected.task_type === "COMMERCIAL_SOLUTION" && selected.status === "pending" && <article><h3>1. 接受商务方案任务</h3><p>商务经理基于已完成的投标准备版本逐条响应商务条款、偏差和风险；本步骤不替代后续核价与价格授权。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "唐商务已接受商务方案与偏差评估责任" }, ["LAB-COMMERCIAL-SOLUTION-ACCEPTANCE"])}>唐商务接受商务方案任务</button></article>}
          {selected.task_type === "TECHNICAL_COLLABORATION" && selected.status === "pending" && <article><h3>1. 接受技术负责人任务</h3><p>接受后，销售项目中的技术负责人状态才从“待接受”变为“已到位”。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "赵工已接受技术负责人责任" }, ["LAB-ACCEPTANCE"])}>赵工接受任务</button></article>}
          {selected.task_type === "REQUIREMENT_BASELINE_PREPARATION" && selected.status === "pending" && <article><h3>1. 接受正式需求基线工作包</h3><p>该角色只负责把已确认的需求澄清包转为版本化正式需求基线，不负责技术方案或BOM。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "需求基线责任人已接受工作包" }, ["LAB-REQBASE-ACCEPTANCE"])}>接受需求基线工作包</button></article>}
          {selected.task_type === "TECHNICAL_SOLUTION_PREPARATION" && selected.status === "pending" && <article><h3>1. 接受技术方案工作包</h3><p>该角色只基于当前有效需求基线形成技术方案，不能代替需求基线或BOM责任人。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "技术负责人已接受技术方案工作包" }, ["LAB-TECHSOL-ACCEPTANCE"])}>接受技术方案工作包</button></article>}
          {selected.task_type === "BOM_AND_CLOSURE_PREPARATION" && selected.status === "pending" && <article><h3>1. 接受BOM与问题闭环工作包</h3><p>该角色负责基于当前技术方案形成报价设计BOM，并记录澄清、偏差及其关闭证据。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "方案设计责任人已接受BOM与问题闭环工作包" }, ["LAB-BOM-ACCEPTANCE"])}>接受BOM工作包</button></article>}
          {selected.task_type === "COSTING_COLLABORATION" && selected.status === "pending" && <article><h3>1. 接受S3核价任务</h3><p>核价人员接受专业作业责任；该操作不会授予销售角色成本或价格审批权。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "刘工已接受核价专业作业责任" }, ["LAB-COSTING-ACCEPTANCE"])}>刘工接受核价任务</button></article>}
          {selected.task_type === "BID_PACKAGE_PREPARATION" && selected.status === "pending" && <article><h3>1. 接受最终提交包编制任务</h3><p>孙投标只负责按G4价格授权和当前业务场景形成提交包；专业评审由另一名评审人完成。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "孙投标已接受最终提交包编制责任" }, ["LAB-BID-PACKAGE-ACCEPTANCE"])}>孙投标接受编制任务</button></article>}
          {selected.task_type === "BID_REVIEW_REMEDIATION" && selected.status === "pending" && <article><h3>1. 接受专业评审问题整改任务</h3><p>整改责任回到投标包责任人。接受任务不会改变原评审结论，也不会让G5自动放行。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "孙投标已接受专业评审问题整改责任" }, ["LAB-BID-REVIEW-REMEDIATION-ACCEPTANCE"])}>孙投标接受整改任务</button></article>}
          {selected.task_type === "G5_SUBMISSION_EXECUTION" && selected.status === "pending" && <article><h3>1. 接受G5正式提交执行任务</h3><p>主管的商务决策已经完成；这里仅按冻结包执行并取得客户/平台回执，不进行第二次审批。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "孙投标已接受按冻结版本正式提交的执行责任" }, ["LAB-G5-SUBMISSION-ACCEPTANCE"])}>孙投标接受提交任务</button></article>}
          {selected.task_type === "COMMERCIAL_RESULT_TRACKING" && selected.status === "pending" && <article><h3>1. {payload.mode === "correction" ? "接受通路结果核实任务" : "接受L2.9商业结果跟踪任务"}</h3><p>{payload.mode === "correction" ? `销售项目Owner对原结果 ${String(payload.priorRouteResultId ?? "—")} 发起核实；投标作业责任人必须引用新的正式来源形成更正版本，不能覆盖历史。` : "结果事实属于投标作业轨；销售项目APP接收并引用，不允许销售员或主管直接伪造结果。"}</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: payload.mode === "correction" ? "孙投标已接受通路结果核实责任" : "孙投标已接受L2.9商业结果跟踪责任" }, ["LAB-RESULT-TRACKING-ACCEPTANCE"])}>孙投标接受{payload.mode === "correction" ? "结果核实" : "结果跟踪"}任务</button></article>}
          {selected.task_type === "CONTRACT_HANDOVER_EXECUTION" && selected.status === "pending" && <article><h3>1. 接受G6中标基线移交任务</h3><p>主管已确认赢单并冻结中标基线；合同管理员只校验接收，不进行第二次商业审批。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "刘倩已接受中标基线接收责任" }, ["LAB-CONTRACT-HANDOVER-ACCEPTANCE"])}>刘倩接受合同移交任务</button></article>}
          {selected.task_type === "DOWNSTREAM_STATUS_TRACKING" && selected.status === "pending" && <article><h3>1. 接受G7下游只读状态跟踪任务</h3><p>该任务只模拟合同、订单、履约、验收、开票和回款系统回传权威状态；不会替销售项目APP生成或修改这些专业事实。</p><button disabled={busy} onClick={() => void send("ExternalTaskAccepted", { acknowledgement: "刘倩已接受下游只读状态跟踪责任" }, ["LAB-DOWNSTREAM-TRACKING-ACCEPTANCE"])}>刘倩接受状态跟踪任务</button></article>}
          {selected.task_type === "BID_PREPARATION" && selected.status === "accepted" && <article><h3>2. 回传投标准备权威版本</h3><p>来源类型和来源编号来自销售项目已登记采购请求，只读锁定；分析、策略、责任分工和时间节点由投标作业形成。</p><div className="form"><label><span>采购请求类型（锁定）</span><input value={bidPreparation.sourceType} readOnly /></label><label><span>采购请求/招标文件引用（锁定）</span><input value={bidPreparation.sourceRef} readOnly /></label>{field("来源版本", bidPreparation.sourceVersion, value => setBidPreparation(current => ({ ...current, sourceVersion: value })))}{field("来源哈希/不可变标识", bidPreparation.sourceHash, value => setBidPreparation(current => ({ ...current, sourceHash: value })))}{field("技术要求摘要", bidPreparation.technicalRequirements, value => setBidPreparation(current => ({ ...current, technicalRequirements: value })))}{field("商务要求摘要", bidPreparation.commercialRequirements, value => setBidPreparation(current => ({ ...current, commercialRequirements: value })))}{field("评审/评标标准", bidPreparation.evaluationCriteria, value => setBidPreparation(current => ({ ...current, evaluationCriteria: value })))}{field("资质要求", bidPreparation.qualificationRequirements, value => setBidPreparation(current => ({ ...current, qualificationRequirements: value })))}{field("已识别风险", bidPreparation.identifiedRisks, value => setBidPreparation(current => ({ ...current, identifiedRisks: value })))}{field("投标策略（区别于销售赢单策略）", bidPreparation.bidStrategy, value => setBidPreparation(current => ({ ...current, bidStrategy: value })))}{bidPreparation.workPlan.map((row, index) => <div className="wide flow-info" key={`${row.task}-${index}`}><strong>计划项 {index + 1}</strong><label><span>任务</span><input value={row.task} onChange={event => setBidPreparation(current => ({ ...current, workPlan: current.workPlan.map((item, itemIndex) => itemIndex === index ? { ...item, task: event.target.value } : item) }))} /></label><label><span>责任角色</span><input value={row.ownerRole} onChange={event => setBidPreparation(current => ({ ...current, workPlan: current.workPlan.map((item, itemIndex) => itemIndex === index ? { ...item, ownerRole: event.target.value } : item) }))} /></label><label><span>截止日期</span><input type="date" value={row.dueDate} onChange={event => setBidPreparation(current => ({ ...current, workPlan: current.workPlan.map((item, itemIndex) => itemIndex === index ? { ...item, dueDate: event.target.value } : item) }))} /></label></div>)}{field("投标准备证据", bidPreparation.evidenceRef, value => setBidPreparation(current => ({ ...current, evidenceRef: value })))}</div><button disabled={busy || !bidPreparation.sourceRef || !bidPreparation.sourceVersion || !bidPreparation.sourceHash} onClick={() => void send("BidPreparationCompleted", bidPreparation, [bidPreparation.sourceRef, bidPreparation.evidenceRef])}>回传投标准备版本</button></article>}
          {selected.task_type === "COMMERCIAL_SOLUTION" && selected.status === "accepted" && <article><h3>2. 回传商务方案权威版本</h3><p>本版本保存商务响应、报价清单引用、偏差和风险；最终报价必须等待后续核价结果与价格授权，不在此处填死。</p><div className="form"><label><span>投标准备版本（锁定）</span><input value={commercialSolution.bidPreparationVersionId} readOnly /></label>{field("付款条款响应", commercialSolution.paymentTerms, value => setCommercialSolution(current => ({ ...current, paymentTerms: value })))}{field("交付条款响应", commercialSolution.deliveryTerms, value => setCommercialSolution(current => ({ ...current, deliveryTerms: value })))}{field("保证金/质保响应", commercialSolution.guaranteeTerms, value => setCommercialSolution(current => ({ ...current, guaranteeTerms: value })))}{field("违约责任响应", commercialSolution.breachLiability, value => setCommercialSolution(current => ({ ...current, breachLiability: value })))}{field("报价策略（待核价授权）", commercialSolution.pricingStrategy, value => setCommercialSolution(current => ({ ...current, pricingStrategy: value })))}{field("报价清单引用", commercialSolution.quotationListRef, value => setCommercialSolution(current => ({ ...current, quotationListRef: value })))}{commercialSolution.commercialDeviations.map((row, index) => <div className="wide flow-info" key={`${row.clause}-${index}`}><strong>商务偏差 {index + 1}</strong><label><span>条款</span><input value={row.clause} onChange={event => setCommercialSolution(current => ({ ...current, commercialDeviations: current.commercialDeviations.map((item, itemIndex) => itemIndex === index ? { ...item, clause: event.target.value } : item) }))} /></label><label><span>响应</span><input value={row.response} onChange={event => setCommercialSolution(current => ({ ...current, commercialDeviations: current.commercialDeviations.map((item, itemIndex) => itemIndex === index ? { ...item, response: event.target.value } : item) }))} /></label><label><span>风险等级</span><select value={row.riskLevel} onChange={event => setCommercialSolution(current => ({ ...current, commercialDeviations: current.commercialDeviations.map((item, itemIndex) => itemIndex === index ? { ...item, riskLevel: event.target.value } : item) }))}><option value="low">低</option><option value="medium">中</option><option value="high">高</option></select></label><label><span>应对措施</span><input value={row.mitigation} onChange={event => setCommercialSolution(current => ({ ...current, commercialDeviations: current.commercialDeviations.map((item, itemIndex) => itemIndex === index ? { ...item, mitigation: event.target.value } : item) }))} /></label></div>)}{field("商务风险评估", commercialSolution.riskAssessment, value => setCommercialSolution(current => ({ ...current, riskAssessment: value })))}{field("风险应对方案", commercialSolution.mitigationPlan, value => setCommercialSolution(current => ({ ...current, mitigationPlan: value })))}{field("商务方案证据", commercialSolution.evidenceRef, value => setCommercialSolution(current => ({ ...current, evidenceRef: value })))}</div><button disabled={busy || !commercialSolution.bidPreparationVersionId} onClick={() => void send("CommercialSolutionCompleted", commercialSolution, [commercialSolution.quotationListRef, commercialSolution.evidenceRef])}>回传商务方案版本</button></article>}
          {selected.task_type === "TECHNICAL_COLLABORATION" && selected.status === "accepted" && <>
            <div className="flow-info"><strong>本任务只负责启动评估</strong><span>完成后系统分别派发正式需求基线、技术方案、BOM与问题闭环三个独立工作包；同一人员即使兼岗，也必须分别接受、回传和留痕。</span></div>
            <article><h3>2. S1方案准备反馈</h3><p>用于G2自动汇总，不在销售阶段门重复填写。</p><div className="form">{field("需求缺失", solution.requirementGaps, value => setSolution(current => ({ ...current, requirementGaps: value })))}{field("技术难度", solution.technicalDifficulty, value => setSolution(current => ({ ...current, technicalDifficulty: value })))}{field("方案资源需求", solution.solutionResourceNeeds, value => setSolution(current => ({ ...current, solutionResourceNeeds: value })))}{field("证据引用", solution.evidenceRef, value => setSolution(current => ({ ...current, evidenceRef: value })))}</div><button disabled={busy || selected.stage !== "S1"} onClick={() => void send("SolutionPreparationCompleted", solution, [solution.evidenceRef])}>{selected.stage === "S1" ? "回传方案准备结果" : "仅S1可回传"}</button></article>
          </>}
          {selected.task_type === "REQUIREMENT_BASELINE_PREPARATION" && selected.status === "accepted" && <article><h3>2. 形成正式需求基线候选</h3><p>把需求澄清包、采购文件和客户确认转为可追溯的标准化需求；这一步先于技术方案。</p>{technicalSnapshotLocked && <div className="flow-blocker"><strong>当前G3提交快照已冻结</strong><span>只有技术评审明确退回后，来源版本才允许更新。</span></div>}<div className="form">{field("标准化需求", technical.standardizedRequirement, value => setTechnical(current => ({ ...current, standardizedRequirement: value })))}{field("需求基线证据", technical.requirementEvidenceRef, value => setTechnical(current => ({ ...current, requirementEvidenceRef: value })))}</div><button disabled={busy || selected.stage !== "S2" || technicalSnapshotLocked} onClick={() => void send("RequirementBaselinePrepared", { standardizedRequirement: technical.standardizedRequirement, sourceRef: technical.requirementEvidenceRef }, [technical.requirementEvidenceRef])}>回传正式需求基线</button></article>}
          {selected.task_type === "TECHNICAL_SOLUTION_PREPARATION" && selected.status === "accepted" && <article><h3>2. 形成技术方案</h3><p>技术方案必须绑定当前有效需求基线；需求基线未形成时，服务端会阻断。</p><div className="form">{field("设计采用值", technical.designAdoptedValue, value => setTechnical(current => ({ ...current, designAdoptedValue: value })))}<label><span>关键参数</span><select value={technical.criticalParameterStatus} onChange={event => setTechnical(current => ({ ...current, criticalParameterStatus: event.target.value }))}><option>完整</option><option>存在缺失</option></select></label>{field("初步技术方案", technical.initialTechnicalSolution, value => setTechnical(current => ({ ...current, initialTechnicalSolution: value })))}{field("方案证据", technical.solutionEvidenceRef, value => setTechnical(current => ({ ...current, solutionEvidenceRef: value })))}</div><button disabled={busy || selected.stage !== "S2" || technicalSnapshotLocked} onClick={() => void send("TechnicalSolutionPrepared", technical, [technical.solutionEvidenceRef])}>回传技术方案版本</button></article>}
          {selected.task_type === "BOM_AND_CLOSURE_PREPARATION" && selected.status === "accepted" && <article><h3>2. 形成设计BOM与澄清偏差闭环</h3><p>BOM绑定当前技术方案；澄清和偏差必须显式闭环，零项也要形成结论。</p><div className="form">{field("报价设计BOM配置", technical.configurationSummary, value => setTechnical(current => ({ ...current, configurationSummary: value })))}<label><span>BOM确认状态</span><select value={technical.bomConfirmationStatus} onChange={event => setTechnical(current => ({ ...current, bomConfirmationStatus: event.target.value }))}><option value="confirmed">已确认</option><option value="unconfirmed">未确认</option></select></label>{field("BOM确认依据", technical.bomEvidenceRef, value => setTechnical(current => ({ ...current, bomEvidenceRef: value })))}{field("澄清问题", technical.clarificationQuestion, value => setTechnical(current => ({ ...current, clarificationQuestion: value })))}{field("澄清答复", technical.clarificationResponse, value => setTechnical(current => ({ ...current, clarificationResponse: value })))}{field("澄清责任角色", technical.clarificationOwnerRole, value => setTechnical(current => ({ ...current, clarificationOwnerRole: value })))}{field("澄清关闭证据", technical.clarificationClosureEvidenceRef, value => setTechnical(current => ({ ...current, clarificationClosureEvidenceRef: value })))}<label><span>澄清状态</span><select value={technical.clarificationStatus} onChange={event => setTechnical(current => ({ ...current, clarificationStatus: event.target.value }))}><option value="resolved">已关闭</option><option value="open">未关闭</option></select></label>{field("偏差描述", technical.deviationDescription, value => setTechnical(current => ({ ...current, deviationDescription: value })))}<label><span>偏差结论</span><select value={technical.deviationStatus} onChange={event => setTechnical(current => ({ ...current, deviationStatus: event.target.value }))}><option value="none">无偏差</option><option value="recorded">存在偏差，已记录</option></select></label>{field("偏差核对证据", technical.deviationEvidenceRef, value => setTechnical(current => ({ ...current, deviationEvidenceRef: value })))}{field("偏差责任角色", technical.deviationOwnerRole, value => setTechnical(current => ({ ...current, deviationOwnerRole: value })))}{field("偏差处理结论", technical.deviationDisposition, value => setTechnical(current => ({ ...current, deviationDisposition: value })))}{field("偏差关闭证据", technical.deviationClosureEvidenceRef, value => setTechnical(current => ({ ...current, deviationClosureEvidenceRef: value })))}</div><button disabled={busy || selected.stage !== "S2" || technicalSnapshotLocked} onClick={() => void send("BomAndClosureCompleted", technical, [technical.bomEvidenceRef, technical.deviationEvidenceRef])}>回传BOM与闭环包</button></article>}
          {selected.task_type === "G3_GATE_REVIEW" && selected.status === "pending" && <article><h3>G3需求与技术方案基线评审</h3><p>技术负责人 {String(payload.preparedBy ?? "—")} 已完成编制。王评审仅对冻结版本形成独立结论，销售员和技术负责人均不能代批。</p>{reviewBlockers.length > 0 && <div className="flow-blocker"><strong>快照包含{reviewBlockers.length}项硬阻断，只允许退回</strong><span>{reviewBlockers.map(item => String(item.label)).join("、")}</span></div>}<label><span>技术评审结论</span><select value={decision.action} disabled={reviewBlockers.length > 0} onChange={event => setDecision(current => ({ ...current, action: event.target.value }))}><option value="approve">评审通过</option><option value="return">退回补充</option></select></label>{field("技术评审意见", decision.comment, value => setDecision(current => ({ ...current, comment: value })))}<button disabled={busy || !reviewId || (reviewBlockers.length > 0 && decision.action !== "return")} onClick={() => void send("G3DecisionSubmitted", { gateId: reviewId, action: decision.action, comment: decision.comment }, [`G3-${reviewId}`])}>{reviewBlockers.length > 0 ? "退回异常快照" : "提交技术评审结论"}</button></article>}
          {selected.task_type === "COSTING_COLLABORATION" && selected.status === "accepted" && <article><h3>2. S3核价专业结果回传</h3><p>核价作业引用已批准技术方案和报价设计BOM，只形成价格快照与核价方案；最终授权底价和对外报价不在此处决定。</p><div className="form">
            {field("价格来源", costing.priceSource, value => setCosting(current => ({ ...current, priceSource: value })))}
            {field("价格来源版本", costing.priceSourceVersion, value => setCosting(current => ({ ...current, priceSourceVersion: value })))}
            {field("币种（ISO三位代码）", costing.currency, value => setCosting(current => ({ ...current, currency: value.toUpperCase() })))}
            {field("税费口径", costing.taxBasis, value => setCosting(current => ({ ...current, taxBasis: value })))}
            {field("贸易/交付条件", costing.tradeTerms, value => setCosting(current => ({ ...current, tradeTerms: value })))}
            {costing.currency !== "CNY" && field("汇率基准、日期与来源", costing.exchangeRateBasis, value => setCosting(current => ({ ...current, exchangeRateBasis: value })))}
            <label><span>价格快照日期</span><input type="date" value={costing.snapshotDate} onChange={event => setCosting(current => ({ ...current, snapshotDate: event.target.value }))} /></label>
            <label><span>价格有效期</span><input type="date" value={costing.validUntil} onChange={event => setCosting(current => ({ ...current, validUntil: event.target.value }))} /></label>
            {field("价格快照证据", costing.priceEvidenceRef, value => setCosting(current => ({ ...current, priceEvidenceRef: value })))}
            {[["材料成本（元）", "materialCostYuan"], ["人工成本（元）", "laborCostYuan"], ["制造成本（元）", "manufacturingCostYuan"], ["运输成本（元）", "transportCostYuan"], ["税费（元）", "taxCostYuan"], ["风险储备（元）", "riskReserveYuan"], ["核价销售价（元）", "costingSalesPriceYuan"], ["目标利润率（%）", "targetProfitRate"]].map(([label, key]) => <label key={key}><span>{label}</span><input type="number" min="0" step="0.01" value={costing[key as keyof typeof costing]} onChange={event => setCosting(current => ({ ...current, [key]: event.target.value }))} /></label>)}
            {field("利润测算依据", costing.calculationBasis, value => setCosting(current => ({ ...current, calculationBasis: value })))}
            {field("交期评估", costing.deliveryAssessment, value => setCosting(current => ({ ...current, deliveryAssessment: value })))}
            {field("交期风险结论", costing.deliveryRiskConclusion, value => setCosting(current => ({ ...current, deliveryRiskConclusion: value })))}
          </div><button disabled={busy || selected.stage !== "S3"} onClick={() => void send("CostingAssessmentCompleted", { ...costing, materialCostYuan: Number(costing.materialCostYuan), laborCostYuan: Number(costing.laborCostYuan), manufacturingCostYuan: Number(costing.manufacturingCostYuan), transportCostYuan: Number(costing.transportCostYuan), taxCostYuan: Number(costing.taxCostYuan), riskReserveYuan: Number(costing.riskReserveYuan), costingSalesPriceYuan: Number(costing.costingSalesPriceYuan), targetProfitRate: Number(costing.targetProfitRate) }, [costing.priceEvidenceRef])}>{selected.stage === "S3" ? "回传S3核价结果" : "仅S3可回传"}</button></article>}
          {selected.task_type === "G4_GATE_REVIEW" && selected.status === "pending" && <article><h3>G4核价与价格授权门确认</h3><p>Gate实例编号 {reviewId}。本任务读取独立价格授权申请；批准时形成授权决定，不覆盖原核价和价格来源。</p>{reviewBlockers.length > 0 && <div className="flow-blocker"><strong>快照包含{reviewBlockers.length}项硬阻断，只允许退回</strong><span>{reviewBlockers.map(item => String(item.label)).join("、")}</span></div>}<label><span>财务授权结论</span><select value={decision.action} disabled={reviewBlockers.length > 0} onChange={event => setDecision(current => ({ ...current, action: event.target.value }))}><option value="approve">确认通过并形成价格授权</option><option value="return">退回补充</option></select></label>{field("授权意见", decision.comment, value => setDecision(current => ({ ...current, comment: value })))}{decision.action === "approve" && <div className="form"><label><span>授权底价（元）</span><input type="number" min="0" value={authorization.floorPriceYuan} onChange={event => setAuthorization(current => ({ ...current, floorPriceYuan: event.target.value }))} /></label><label><span>授权报价（元）</span><input type="number" min="0" value={authorization.authorizedQuotePriceYuan} onChange={event => setAuthorization(current => ({ ...current, authorizedQuotePriceYuan: event.target.value }))} /></label>{field("授权币种", authorization.currency, value => setAuthorization(current => ({ ...current, currency: value.toUpperCase() })))}{field("授权税费口径", authorization.taxBasis, value => setAuthorization(current => ({ ...current, taxBasis: value })))}{field("授权贸易/交付条件", authorization.tradeTerms, value => setAuthorization(current => ({ ...current, tradeTerms: value })))}{field("授权报价通路（多个用逗号）", authorization.routeScope, value => setAuthorization(current => ({ ...current, routeScope: value })))}{field("授权例外与失效条件", authorization.exceptionConditions, value => setAuthorization(current => ({ ...current, exceptionConditions: value })))}{field("项目范围与报价范围核对结论", authorization.scopeAlignmentConclusion, value => setAuthorization(current => ({ ...current, scopeAlignmentConclusion: value })))}{field("项目预计金额与授权报价差异说明", authorization.amountVarianceExplanation, value => setAuthorization(current => ({ ...current, amountVarianceExplanation: value })))}<label><span>授权有效期</span><input type="date" value={authorization.validUntil} onChange={event => setAuthorization(current => ({ ...current, validUntil: event.target.value }))} /></label>{field("价格授权证据", authorization.evidenceRef, value => setAuthorization(current => ({ ...current, evidenceRef: value })))}</div>}<button disabled={busy || !reviewId || (reviewBlockers.length > 0 && decision.action !== "return")} onClick={() => void send("G4DecisionSubmitted", { gateId: reviewId, action: decision.action, comment: decision.comment, ...(decision.action === "approve" ? { ...authorization, floorPriceYuan: Number(authorization.floorPriceYuan), authorizedQuotePriceYuan: Number(authorization.authorizedQuotePriceYuan) } : {}) }, [`G4-${reviewId}`, authorization.evidenceRef])}>{reviewBlockers.length > 0 ? "退回异常快照" : "提交核价与价格授权结论"}</button></article>}
          {selected.task_type === "BID_PACKAGE_PREPARATION" && selected.status === "accepted" && <article><h3>2. S4最终提交包回传</h3><p>提交类型必须匹配项目场景，报价必须等于G4授权报价且不得低于底价。本步骤只形成投标包，不填写专业评审结论。</p><div className="form"><label><span>提交类型</span><select value={bidPackage.submissionType} onChange={event => setBidPackage(current => ({ ...current, submissionType: event.target.value }))}><option value="DirectBidSubmission">客户直接投标</option><option value="CustomerRouteQuotation">EPC客户询价通路报价</option><option value="DirectQuotation">客户直接询价报价</option></select></label>{field("提交通路编号", bidPackage.routeId, value => setBidPackage(current => ({ ...current, routeId: value })))}<label><span>最终报价（元）</span><input type="number" min="0" value={bidPackage.quotedPriceYuan} onChange={event => setBidPackage(current => ({ ...current, quotedPriceYuan: event.target.value }))} /></label>{field("提交包哈希", bidPackage.packageHash, value => setBidPackage(current => ({ ...current, packageHash: value })))}{field("投标包形成证据", bidPackage.evidenceRef, value => setBidPackage(current => ({ ...current, evidenceRef: value })))}</div><button disabled={busy || selected.stage !== "S4"} onClick={() => void send("BidPackagePrepared", { ...bidPackage, quotedPriceYuan: Number(bidPackage.quotedPriceYuan) }, [bidPackage.evidenceRef, bidPackage.packageHash])}>回传最终提交包</button></article>}
          {selected.task_type === "BID_PACKAGE_REVIEW" && selected.status === "pending" && <article><h3>S4投标包｜{String(payload.reviewLabel ?? "专业评审")}</h3><p>投标包 {String(payload.bidPackageVersionId ?? "—")} 由投标专员形成。本任务只允许 {selected.assignee_name} 按冻结的“{String(payload.reviewLabel ?? "专业评审")}”维度形成独立结论，不能代替其他专业。</p><div className="form"><label><span>评审类型（任务冻结）</span><input value={String(payload.reviewLabel ?? professionalReview.reviewType)} readOnly /></label><label><span>专业评审结论</span><select value={professionalReview.reviewConclusion} onChange={event => setProfessionalReview(current => ({ ...current, reviewConclusion: event.target.value }))}><option value="approved">通过</option><option value="rejected">不通过</option></select></label><label><span>未关闭问题数</span><input type="number" min="0" step="1" value={professionalReview.openRiskCount} onChange={event => setProfessionalReview(current => ({ ...current, openRiskCount: event.target.value }))} /></label><label><span>偏差处理</span><select value={professionalReview.deviationConclusion} onChange={event => setProfessionalReview(current => ({ ...current, deviationConclusion: event.target.value }))}><option value="none">无偏差</option><option value="authorized">存在偏差，提交G5逐项授权</option></select></label>{field("专业评审意见（存在偏差时须写明事项与影响）", professionalReview.reviewSummary, value => setProfessionalReview(current => ({ ...current, reviewSummary: value })))}{field("专业评审证据", professionalReview.evidenceRef, value => setProfessionalReview(current => ({ ...current, evidenceRef: value })))}</div><button disabled={busy || !payload.bidPackageVersionId || !professionalReview.reviewSummary.trim()} onClick={() => void send("BidPackageReviewSubmitted", { bidPackageVersionId: String(payload.bidPackageVersionId), ...professionalReview, reviewType: String(payload.reviewType), openRiskCount: Number(professionalReview.openRiskCount) }, [professionalReview.evidenceRef, String(payload.packageHash ?? "")])}>提交{String(payload.reviewLabel ?? "专业评审")}结论</button></article>}
          {selected.task_type === "BID_REVIEW_REMEDIATION" && selected.status === "accepted" && <article><h3>S4投标包｜{String(payload.reviewLabel ?? "专业评审")}问题整改</h3><p>原评审 {String(payload.professionalReviewId ?? "—")} 已保留。投标专员只能提交整改说明和证据，不能自行把问题改成“已通过”；系统会重新派给原评审人复核。</p><div className="form"><label><span>原评审意见</span><textarea value={String(payload.reviewSummary ?? "—")} readOnly /></label>{field("逐项整改说明", reviewRemediation.issueResponse, value => setReviewRemediation(current => ({ ...current, issueResponse: value })))}{field("整改证据", reviewRemediation.evidenceRef, value => setReviewRemediation(current => ({ ...current, evidenceRef: value })))}</div><button disabled={busy || !payload.professionalReviewId || !reviewRemediation.issueResponse.trim() || !reviewRemediation.evidenceRef.trim()} onClick={() => void send("BidReviewRemediationSubmitted", { professionalReviewId: String(payload.professionalReviewId), ...reviewRemediation }, [reviewRemediation.evidenceRef])}>提交整改并请求原评审人复核</button></article>}
          {selected.task_type === "G5_SUBMISSION_EXECUTION" && selected.status === "accepted" && <article><h3>2. 按G5冻结版本正式提交并回传回执</h3><p>Gate实例编号 {reviewId}。包哈希来自主管批准快照，不允许修改；单条通路回执只完成该通路，冻结批次全部通路回执到齐后主系统才从S4进入S5。</p><div className="form"><label><span>冻结包哈希</span><input value={String(payload.packageHash ?? "")} readOnly /></label><label><span>正式提交时间</span><input type="datetime-local" value={receipt.submittedAt} onChange={event => setReceipt(current => ({ ...current, submittedAt: event.target.value }))} /></label>{field("客户/平台回执编号", receipt.receiptRef, value => setReceipt(current => ({ ...current, receiptRef: value })))}{field("回执哈希", receipt.receiptHash, value => setReceipt(current => ({ ...current, receiptHash: value })))}</div><button disabled={busy || !reviewId || !payload.packageHash} onClick={() => void send("CommercialSubmissionAccepted", { gateId: reviewId, packageHash: String(payload.packageHash), submittedAt: new Date(receipt.submittedAt).toISOString(), receiptRef: receipt.receiptRef, receiptHash: receipt.receiptHash }, [receipt.receiptRef, receipt.receiptHash])}>回传该通路正式提交回执</button></article>}
          {selected.task_type === "COMMERCIAL_RESULT_TRACKING" && selected.status === "accepted" && <article><h3>2. {payload.mode === "correction" ? "回传通路结果更正事实" : "回传该报价通路的L2.9正式商业结果"}</h3><p>{payload.mode === "correction" ? `本次必须引用原结果 ${String(payload.priorRouteResultId ?? "—")}。更正成功后原记录保留为superseded，新记录成为有效版本并触发项目结果重新汇总。` : `本任务只对应通路“${String(payload.routeId ?? "DIRECT")}”。单条通路结果不会直接决定整个销售项目；全部已提交通路结果齐备后，主系统才汇总项目级结果并开放G6。`}</p><div className="form"><label><span>报价通路（冻结）</span><input value={String(payload.routeId ?? "DIRECT")} readOnly /></label><label><span>正式提交记录（冻结）</span><input value={String(payload.commercialSubmissionId ?? "—")} readOnly /></label>{payload.mode === "correction" && <><label><span>原结果编号</span><input value={String(payload.priorRouteResultId ?? "—")} readOnly /></label><label><span>原结果类型</span><input value={String(payload.priorResultType ?? "—")} readOnly /></label>{field("正式更正原因", commercialResult.correctionReason, value => setCommercialResult(current => ({ ...current, correctionReason: value })))}</>}<label><span>结果类型</span><select value={commercialResult.resultType} onChange={event => setCommercialResult(current => ({ ...current, resultType: event.target.value }))}><option value="won">该通路中标（该EPC选用钱江）</option><option value="lost">该通路未赢（该EPC未中标或未选用钱江）</option></select></label>{field("结果来源编号", commercialResult.sourceRef, value => setCommercialResult(current => ({ ...current, sourceRef: value })))}<label><span>结果通知日期</span><input type="date" value={commercialResult.noticeDate} onChange={event => setCommercialResult(current => ({ ...current, noticeDate: event.target.value }))} /></label>{field("结果证据", commercialResult.evidenceRef, value => setCommercialResult(current => ({ ...current, evidenceRef: value })))}{commercialResult.resultType === "won" ? field("中标范围", commercialResult.awardScope, value => setCommercialResult(current => ({ ...current, awardScope: value }))) : field("中标竞争方或未知说明", commercialResult.competitorName, value => setCommercialResult(current => ({ ...current, competitorName: value })))}</div><button disabled={busy || !payload.routeId || (payload.mode === "correction" && !commercialResult.correctionReason.trim())} onClick={() => void send("CommercialResultReported", { ...commercialResult, routeId: String(payload.routeId), correctionOfResultId: payload.mode === "correction" ? String(payload.priorRouteResultId) : "" }, [commercialResult.sourceRef, commercialResult.evidenceRef])}>回传{payload.mode === "correction" ? "更正后" : "该通路"}正式结果事实</button></article>}
          {selected.task_type === "CONTRACT_HANDOVER_EXECUTION" && selected.status === "accepted" && <>
            <article><h3>2A. 校验通过并回传合同接收回执</h3><p>Gate实例编号 {reviewId}。中标基线编号和哈希来自G6批准快照，不允许修改；只有回执匹配才会推进S6。</p><div className="form"><label><span>中标基线编号</span><input value={String(payload.awardBaselineId ?? "")} readOnly /></label><label><span>基线清单哈希</span><input value={String(payload.manifestHash ?? "")} readOnly /></label>{field("合同侧参考号", contractReceipt.contractRef, value => setContractReceipt(current => ({ ...current, contractRef: value })))}{field("合同接收回执编号", contractReceipt.receiptRef, value => setContractReceipt(current => ({ ...current, receiptRef: value })))}{field("接收回执哈希", contractReceipt.receiptHash, value => setContractReceipt(current => ({ ...current, receiptHash: value })))}<label><span>接收时间</span><input type="datetime-local" value={contractReceipt.receivedAt} onChange={event => setContractReceipt(current => ({ ...current, receivedAt: event.target.value }))} /></label></div><button disabled={busy || !reviewId || !payload.awardBaselineId || !payload.manifestHash} onClick={() => void send("ContractHandoverAccepted", { gateId: reviewId, awardBaselineId: String(payload.awardBaselineId), manifestHash: String(payload.manifestHash), contractRef: contractReceipt.contractRef, receiptRef: contractReceipt.receiptRef, receiptHash: contractReceipt.receiptHash, receivedAt: new Date(contractReceipt.receivedAt).toISOString() }, [contractReceipt.receiptRef, contractReceipt.receiptHash])}>回传合同接收回执并推进S6</button></article>
            <article><h3>2B. 发现承诺差异并退回</h3><p>退回不会推进S6，也不会覆盖已批准中标基线；销售项目必须定位差异来源并形成新的受控版本。</p><div className="form">{field("合同与最终承诺差异", contractReturn.differenceSummary, value => setContractReturn(current => ({ ...current, differenceSummary: value })))}{field("整改要求", contractReturn.remediationRequirement, value => setContractReturn(current => ({ ...current, remediationRequirement: value })))}{field("退回证据", contractReturn.evidenceRef, value => setContractReturn(current => ({ ...current, evidenceRef: value })))}</div><button disabled={busy || !reviewId || !payload.awardBaselineId || !payload.manifestHash || !contractReturn.differenceSummary.trim() || !contractReturn.remediationRequirement.trim() || !contractReturn.evidenceRef.trim()} onClick={() => void send("ContractHandoverReturned", { gateId: reviewId, awardBaselineId: String(payload.awardBaselineId), manifestHash: String(payload.manifestHash), ...contractReturn }, [contractReturn.evidenceRef])}>退回中标基线并阻止推进</button></article>
          </>}
          {selected.task_type === "DOWNSTREAM_STATUS_TRACKING" && selected.status === "accepted" && <article><h3>2. 回传一条下游权威状态</h3><p>状态原文按来源系统入账并只读展示。当前尚未确认G7经营关闭阈值，因此本测试台不会提供“关闭项目”按钮。</p><div className="form"><label><span>业务对象</span><select value={downstream.eventType} onChange={event => setDownstream(current => ({ ...current, eventType: event.target.value }))}><option value="ContractStatusReported">合同</option><option value="OrderStatusReported">订单</option><option value="DeliveryStatusReported">履约/交付</option><option value="AcceptanceStatusReported">验收</option><option value="InvoiceStatusReported">开票</option><option value="PaymentStatusReported">回款</option></select></label>{field("对象编号", downstream.objectRef, value => setDownstream(current => ({ ...current, objectRef: value })))}{field("来源状态原文", downstream.businessStatus, value => setDownstream(current => ({ ...current, businessStatus: value })))}<label><span>金额（元，可选）</span><input type="number" min="0" value={downstream.amountYuan} onChange={event => setDownstream(current => ({ ...current, amountYuan: event.target.value }))} /></label>{field("证据引用", downstream.evidenceRef, value => setDownstream(current => ({ ...current, evidenceRef: value })))}</div><button disabled={busy || !downstream.objectRef.trim() || !downstream.businessStatus.trim() || !downstream.evidenceRef.trim()} onClick={() => void send("DownstreamBusinessStatusReported", { eventType: downstream.eventType, objectRef: downstream.objectRef, businessStatus: downstream.businessStatus, amountYuan: downstream.amountYuan ? Number(downstream.amountYuan) : undefined, currency: downstream.currency, evidenceRef: downstream.evidenceRef }, [downstream.evidenceRef])}>回传只读状态事实</button></article>}
          {["completed", "returned", "rejected", "cancelled"].includes(selected.status) && <div className="empty large">任务已结束，结果已写入主系统。请在3000刷新查看。</div>}
        </>}
      </section>
    </main>
    <section className="logs"><div><h2>入站事件日志</h2>{state.events.map((event, index) => <p key={`${event.event_id}-${index}`}><strong>{String(event.event_type)}</strong><span>{String(event.project_code)} · {String(event.processing_status)}</span><small>{String(event.processing_error ?? event.received_at ?? "")}</small></p>)}</div><div><h2>出站任务日志</h2>{state.outbox.map((event, index) => <p key={`${event.event_id}-${index}`}><strong>{String(event.event_type)}</strong><span>{String(event.external_task_id)} · {String(event.status)}</span><small>{String(event.created_at ?? "")}</small></p>)}</div></section>
  </div>;
}

createRoot(document.getElementById("root")!).render(<React.StrictMode><App /></React.StrictMode>);
