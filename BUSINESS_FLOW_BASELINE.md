# 销售项目业务流工程基线

权威来源：`DP-02钱江电气销售项目与投标领域本体包V1.2双轨版.html`。

线索与投标接口补充基线：`LEAD_BID_INTEGRATION_BASELINE.md`。修改批次和未完成事项统一记录在 `CHANGE_BACKLOG.md`，不得以新接口工作覆盖或遗忘既有整改项。

本文件用于约束当前 Demo 工程实现，不替代权威来源。权威文件未冻结的提交岗位和审核 SLA 不得在代码中伪造为已确认事实；S0–S6 与 G1–G7 的严格映射由下述 Gate 补充基线单独治理。

Gate 补充基线：`LIFECYCLE_GATE_BASELINE.md`。经当前方案决策，原始 V1.2 中未与 S0–S6 对齐的 Gate 口径允许重新定义；涉及 Gate 编号、名称、阶段归属、人员确认、审批和阶段迁移时，以该补充基线为准。除 Gate 口径外，三类场景、L2.1–L2.9、双轨活动、对象血缘、系统边界和业务规则继续以原始 V1.2 为准。

## 核心约束

1. SalesProject 是唯一聚合根；三种场景共享 SalesProject、S0–S6、统一生命周期和审计结构。
2. 经营事实由销售项目管理侧记录；投标、需求、方案、BOM、核价、评审、提交及结果事实由投标作业或专业作业侧记录。
3. 阶段门引用已有业务对象及版本，只保存引用快照、申请说明和决策，不得成为重复录入业务事实的表单。
4. 每个 S 阶段设置一个用户可见的联合出口 Gate；阶段出口判定与双轨事实交互在同一 Gate 实例内汇总，但经营事实、专业事实、审批决策和实际执行事件仍分层保存。
5. 销售项目 Owner 推动、协调和催办，不替技术、采购、财务、价格授权人形成或批准专业结论。
6. 钉钉当前只作为通知和任务入口；权威审批结论与审计仍保存在销售项目系统。
7. 正式版本和提交快照不可覆盖；补充或变更必须形成新版本。

## 场景编码

权威文件中的场景 S1/S2/S3 与项目阶段 S0–S6 重名。工程内部使用以下无歧义编码：

| 工程编码 | 中文场景 | ProcurementRequest | 对外提交 |
| --- | --- | --- | --- |
| SCN-01-DIRECT-BID | 客户直接投标 | TenderRequest | DirectBidSubmission |
| SCN-02-EPC-INQUIRY | EPC客户询价 | EPCInquiry | CustomerRouteQuotation |
| SCN-03-DIRECT-RFQ | 客户直接询价 | DirectRFQ | DirectQuotation |

## 已确认的阶段责任

| 阶段 | 核心输出 | 退出事件 | 确认/审批责任 |
| --- | --- | --- | --- |
| S0 待立项 | 项目基本信息、初始标的、Owner、预计金额 | SalesProjectEstablished | 销售主管 |
| S1 立项与策略 | 项目价值、客户地图、竞争/策略、资源计划 | StrategyApproved | 销售主管/VP |
| S2 需求与方案 | 客户需求版本、技术澄清、初步方案、偏差清单 | CustomerRequirementBaselined | 技术负责人 |
| S3 核价评审 | 核价技术方案、成本、储备、交期、毛利 | CostingSolutionApproved | 财务/授权人 |
| S4 定价与投标 | 定价授权、投标方案、专业评审、偏差、冻结提交包、正式回执 | BidDecisionMade + CommercialSubmissionAccepted | 销售主管/投标授权决策人；投标专员执行 |
| S5 结果与移交 | 结果、复盘、合同移交基线 | Won/Lost/Terminated | 销售主管 |
| S6 赢单后跟踪 | 合同、订单、履约、开票、回款只读状态 | CommercialProjectClosed | 销售项目Owner |

## 阶段门映射状态

- G1–G7 已按 `LIFECYCLE_GATE_BASELINE.md` 重新定义，并与 S0–S6 的阶段出口一一对应。
- 新 G3 为“需求方案基线门”；原 V1.2 G3 的核价定价控制调整到新 G4。
- 原 V1.2 G4 投标决策与 G5 提交控制合并为新 G5“商务决策与提交门”：前台一张 Gate、一次主要审批，后台分别保留决策、执行提交和客户回执事实。
- 新 G7 在继承成交后经营跟踪的基础上补充经营关闭判定。
- 原始 L2.1–L2.9 顺序、双轨专业活动和业务控制不因 Gate 重新编号而删除或改序。

## 当前迭代

已完成 G2 来源对象汇总，并将原有 S2/S3“阶段出口”运行态正式归并为新 G3“需求方案基线门”和 G4“核价授权门”；G5已实现一次商务决策和后续受控提交执行；G6已实现正式结果事实、结构化未成交复盘、中标基线冻结及合同接收执行。销售Owner形成销售侧来源并发起Gate；技术负责人编制、技术评审人独立确认G3；财务/价格授权人确认G4；投标专员编制提交包、专业评审人独立形成G5输入。销售主管批准G5后项目保持S4，投标作业回传有效回执后才迁移S5。S5由投标作业轨按已提交报价通路分别回传 `QuotationRouteResult`，销售项目APP在全部通路齐备且无多赢冲突后确定性汇总项目级 `BidResult`；销售Owner再提交自动汇总的G6，销售主管只确认一次。赢单时项目保持S5并等待合同APP按基线哈希接收，回执匹配后进入S6；全部通路未赢或终止时必须完成结构化复盘，G6批准后Closed。旧 G3/G4/G5 语义及原阶段出口表仅保留为迁移审计历史，不再参与运行态查询和写入。

立项入口现只允许选择上述三种权威场景，并持久化稳定场景编码、采购请求类型和提交对象类型。EPC场景已实现一个项目多条 `QuotationRoute`、统一项目授权报价、通路级受控价格例外、G5全通路批次冻结与回执聚合，以及S5逐通路结果回传和项目级结果汇总；其他场景差异化字段、判重规则和完整页面翻译仍需后续建设，不得宣称三类场景均已完整实现。

外部专业作业采用“主系统3000 + 本地集成测试台3010”隔离架构。3000只保留销售员和销售主管的业务动作与外部结果只读投影；3010集中模拟技术负责人、核价人员、价格授权人、投标专员和合同管理员的任务接受、专业结果与受控回执。两端通过 `external_tasks`、集成发件箱、幂等收件箱和处理日志衔接，事件必须标记 `environment=demo`、`simulated=true`。此测试台用于当前无外部系统条件下验证契约和业务连续性，不等同于已经连接真实钉钉、投标或合同系统。

## 业务数据源收口（2026-08-23）

- 默认业务模式以D1为唯一项目集合，不再将 `initialProjects` 静态样例追加到API结果。
- D1不可用时必须显性失败，不允许静默回退到样例并继续产生看似真实的任务、审批或统计。
- 静态样例只能通过 `/?data=sample` 显式进入，并持续标识为样例模式；样例重置不得修改D1。
- 主管阶段门中心只显示真实待处理Gate实例；销售工作台与行动中心只引用D1活动、角色指派、关系缺口、偏差和版本状态计算任务，不再按固定项目编号生成预设事项。
- 普通ACT-SPM经营行动已经持久化，并使用显式状态机、乐观锁、幂等命令、证据引用和不可变流转账本；外部ACT-BID仍只保存只读投影。
- 关键资源采用“资源需求 → 候选快照 → 主管确认指派 → 被指派人接受责任 → 到位”五事实模型。需求可由Owner发起，也可由主管基于阶段和项目事实主动建立，但主管主动配置时必须同时生成可审计的需求记录，不能直接写入“已到位”。候选快照必须记录来源、能力、负荷、可到位日期和证据；当前本地候选数据必须标识为模拟资源目录，不得冒充企业资源池。
- 本系统内可配置的经营协同角色暂为技术负责人、高层伙伴、渠道经理和服务协调。核价、价格授权、投标、合同等专业角色只能由外部作业任务投影，销售主管不得在项目卡片中手工替代专业系统指派或审批。
- “附条件批准”不是普通审批按钮。企业尚未定义条件责任人、到期日、逾期处置、阶段影响和审计规则前，运行态不提供该决策；不得用一条自由文本意见模拟完整状态机。
- 项目经营风险已迁移到D1权威台账。没有风险记录显示“待评估”，不得等同于低风险；风险必须记录事实、影响、责任、期限和证据，转为行动时原子建立关联，关闭必须由项目Owner提交处置结论和关闭证据。
- G5把未关闭项目风险作为经营决策来源冻结。高/重大风险不新增审批门；主管若继续投/报，必须在同一次G5中显式承接并说明控制依据，承接记录与风险版本和Gate版本绑定，风险仍保持原状态继续处置。G5提交后风险版本变化会使快照失效并要求重提。
- 客户主数据匹配、伙伴验证、资源主数据/负荷、G7经营关闭和下游合同/订单/履约/开票/回款仍未完成真实数据闭环，不得宣称全部经营数据均已持久化。

## 经营事实收口验收（2026-08-23）

- 本地迁移 `0024_sales_operating_register.sql` 与 `0025_g5_residual_risk_acceptance.sql` 已成功应用，新增资源/风险经营台账及 `gate_risk_acceptances` G5剩余风险承接记录。
- 完整构建、测试台构建、类型检查、Lint和41项自动化测试通过。
- 正式HTTP接口回放验证候选登记、主管基于候选确认指派后，候选为 `selected`、需求和指派均为“待接受”；只有被指派人在独立测试台接受任务后，才能进一步形成“已到位”。
- 正式HTTP接口回放验证风险登记、同事务创建关联行动、Owner关闭后状态为 `closed` 且版本从1推进到3；主管代Owner关闭返回403。
- 独立D1端到端回放验证：含高风险项目可由Owner提交G5；主管未显式承接时批准返回409，补充承接依据后同一G5批准成功；风险仍保持未关闭，且生成一条绑定风险版本与Gate版本的承接记录。
- 浏览器页面级核验已覆盖资源候选链、G6统一入口和立项字段业务解释；运行期间控制台无错误。全场景视觉回归与字号、布局整改仍需在后续批次完成。

## G6 验收记录（2026-08-22）

- 已在隔离 D1 数据库中通过正式业务 API 和受控集成事件完整回放 G1–G6，不使用业务表直写推进阶段。
- 未中标分支已验证：L2.9结果回传 → 销售Owner结构化复盘 → G6提交 → 主管退回 → Owner补充 → 同一Gate版本升级重提 → 主管确认 → `S5 + Lost + Closed`。
- 不投/终止分支已验证：G5经营决策不投 → `S5 + Terminated + PendingClose` → Owner结构化复盘 → G6主管确认 → `S5 + Terminated + Closed`。
- 幂等已验证：同一 `CommercialResultReported.eventId` 重复回传只返回既有处理结果，不重复写入。
- 权限已验证：销售主管不能代项目Owner提交G6，接口返回403。
- 赢单哈希控制已验证：合同侧回传错误 `manifestHash` 时返回409；项目保持 `S5 + Won + PendingClose`，G6保持 `approved/executing`，合同回执为0条，拒绝事件留存在集成收件箱，不得推进S6。
- 本地运行器风险：Windows下Wrangler 4.124在Node验收客户端收到预期4xx后可能出现代理连接中断；这是当前本地运行器兼容性问题，不改变业务事务的拒绝结果。部署前需在目标Cloudflare环境重新执行同一验收脚本，并决定是否升级运行器版本。

## EPC多通路结果验收记录（2026-08-23）

- 已在独立D1验收数据库中通过正式业务API、Gate决策与集成事件回放三条完整分支；没有直接写业务表推进阶段。
- 一赢一失：两条G5冻结通路分别取得提交回执，首条结果回传后项目保持 `S5 + Pending` 且G6返回409；第二条结果齐备后汇总为项目级 `won`，G6主管确认、合同APP按基线哈希接收后进入S6。
- 全部未赢：两条通路均为 `lost` 后汇总为项目级 `lost`；结构化复盘齐套并由G6主管确认后形成 `S5 + Lost + Closed`。
- 多条同时中标：两条通路结果均保留，项目保持 `S5 + Pending + Active`，不生成项目级结果通知、不创建G6实例，并以 `resultAggregationStatus=conflict` 阻断人工任意选路。销售Owner发起指定通路结果核实后，投标责任人通过独立任务回传带原结果引用、原因和新证据的更正版；旧结果转为 `superseded`，冲突解除后系统重新汇总并开放G6。
- 数据库核对：混合与全失标分支各有2条有效通路结果；冲突更正分支保留2条初始结果和1条更正结果的完整版本链，且任一通路始终只有1条有效结果。
- 可重复命令：先以独立持久化目录启动验收服务，再运行 `node tests/g6-acceptance.e2e.mjs epc http://127.0.0.1:<验收端口>`。

## EPC G5冻结批次变更验收记录（2026-08-23）

- 零回执解冻：双通路G5批准后、投标侧尚未提交时，Owner提出撤回通路解冻申请；重复申请保持幂等。主管批准后原G5 V1决策历史保留，2项旧提交任务转为cancelled，Gate回到returned，冻结包恢复为prepared。
- 受控撤回与重提：Owner依据EPC撤回通知将指定通路标记withdrawn；对应包转为superseded。剩余通路重新形成G5 V2并由主管再次授权，只生成1项新版本提交任务，项目仍保持S4等待真实回执。
- 回执后锁定：双通路批次第一条正式回执入账后项目仍处于S4，但任何G5解冻申请均返回409；系统不以“仍在S4”为由覆盖已经发生的外部提交事实。
- 已确认边界：上述流程只解决当前G5版本零回执时的受控回退。首条回执后的后到询价、通路撤回及跨批次汇总仍需企业规则，不宣称已经实现。
