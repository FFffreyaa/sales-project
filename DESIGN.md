# 钱江电气销售项目管理 APP｜V0.1 Design System

## 1. 设计目标

- 面向传统制造企业销售团队，优先表达责任、证据、风险与决策，不做消费化或科幻化界面。
- 以角色任务组织页面：销售员看到“下一步做什么”，销售主管看到“哪里需要决策、配置资源和纠偏”。
- 复杂业务采用工作区、对象主页、对比矩阵和审计时间线，避免把全部字段堆在同一层级。
- 状态必须同时包含文字、图标与颜色；颜色永远不是唯一信息载体。

## 2. 视觉基调

- Shell：深海军蓝固定侧栏，白色顶栏，暖灰工作区。
- 主色：`#1769e0`，用于当前状态、主操作和可点击链接。
- 成功：`#16865a`；警告：`#c77a12`；阻断：`#cf3f3f`；中性：`#66758a`。
- 页面背景：`#f4f6f9`；卡片：`#ffffff`；边线：`#dfe5ec`。
- 圆角 6～10px；阴影极轻；禁止霓虹、玻璃拟态、渐变大背景和装饰性大图表。

## 3. 字体与密度

- 字体：`Microsoft YaHei`, `PingFang SC`, `Inter`, system-ui。
- 页面标题 24～30px；区域标题 15～17px；正文 12～14px；辅助信息 10～12px。
- 桌面页面采用 12 列逻辑网格；表格行高 42～52px；关键操作始终在对象标题或区域标题右侧。

## 4. 页面模式

- 工作台：角色契约 → KPI → 决策/行动队列 → 阶段分布 → 风险和版本异常。
- 项目 360：对象头 → S0～S6 阶段轨道 → 页签 → 当前阶段工作区。
- 关系地图：决策路径 → 三层关系卡片 → 缺口与证据闭环。
- 运作看板：战略任务泳道 → 缺口—动作—责任—证据矩阵。
- 版本治理：版本链 → 一致性矩阵 → 偏差审批 → 版本时间线。
- 阶段门：条件与输出 → 阻断/例外 → 决策影响 → 审批意见和审计。
- 结果移交：中标基线清单 → 接收路径 → 下游只读状态；丢标则进入结构化复盘。

## 5. 角色边界

- 销售员：维护事实、补充关系证据、新增行动、申请资源、发起阶段门和结果确认申请。
- 销售主管：立项决策、资源配置、偏差审批、阶段推进/退回/终止、确认中标或丢标结果。
- 投标管理 APP：负责投标文件具体编制作业；本 APP 仅引用状态、负责人、版本、哈希和客户回执。
- 合同、订单、交付、开票、回款：仅展示下游系统的只读权威事实。

## 6. 交互规则

- 主操作必须给出成功反馈、阻断原因或可信的演示边界说明。
- 已批准或正式提交版本不可覆盖；需求变更创建新版本并传播 ReviewRequired。
- 未批准偏差、失效下游版本和必需资源缺失会阻止正式提交。
- 阶段是管理标签，不等于任务状态；阶段门中必须同时显示任务、风险、版本和责任人。
