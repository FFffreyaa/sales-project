# 销售项目管理系统（sales-project-demo）问题与改进清单

> 汇总日期：2026-09-04
> 检查方式：全程只读，未修改任何项目代码
> 说明：本清单汇总多轮视角检查的发现——安全/鉴权、业务规则、产品可用性、销售员/销售主管角色代入、跨目录补充检查，按主题归并为七个部分。严重度按「生产阻断 > 业务规则矛盾 > 演示可信度 > 可用性细节」排序。

---

## 目录

1. 安全与鉴权（生产上线前硬阻断项）
2. 业务规则事实源矛盾（文档与代码契约对齐）
3. 产品/可用性改进（全局）
4. 单项目详情页卡片级改进
5. 销售员视角使用摩擦
6. 销售主管视角使用摩擦
7. 跨目录补充检查（集成实验室 + 操作工作区）

---

## 一、安全与鉴权（生产上线前硬阻断项）

### 1.1 演示身份头可被任意伪造，无真实认证

- **位置**：`app/api/_lib/p0-flow.ts` 的 `requireDemoActor()`
- **问题**：直接读 `x-demo-actor-id` 请求头，从硬编码名单里匹配角色返回。任何调用方把该头改成 `manager-zhou`、`authorizer-luo`、`technical-reviewer-wang` 等即可冒充对应角色执行审批/核价/评审。
- **必要修改**：接入企业 SSO/钉钉身份，由服务端映射角色与数据范围，前端不再决定身份。

### 1.2 集成接口使用硬编码共享密钥

- **位置**：`app/api/integration/{lab,bid,lead}/events/route.ts` 的 `authorized()`、`app/api/p0/projects/route.ts` 的 bypass 判断
- **问题**：写死 `qj-local-integration-lab`，且非恒定时间字符串比较。知道该值即可：① 注入技术/核价/投标/合同事件；② 绕过「销售项目只能由待转化线索生成」的限制直接建项目（`acceptanceBypass`）。CORS 只限浏览器来源 `127.0.0.1:3010`，挡不住 curl 等非浏览器客户端。
- **必要修改**：密钥改从环境变量/密钥托管读取，加签名与防重放，区分「验收钩子」与真实集成通道。

### 1.3 `app/chatgpt-auth.ts` 直接信任 `oai-authenticated-*` 请求头

- **问题**：这是 ChatGPT 认证代理模式，前提是前面有可信代理注入并剥离这些头。当前 demo 实际用 `x-demo-actor-id`，此文件疑似未真正接线。
- **需确认**：是否还有调用方；若部署时无可信代理，这些头可被伪造。

### 1.4 敏感文件与日志会随仓库提交（`.gitignore` 漏网）

- `.openai/hosting.json` 含 `project_id`（`appgprj_6a82…`），但 `.gitignore` 只忽略 `.openai/site-build.tar.gz`，此文件会被提交。
- 下列运行日志/PID 不匹配现有 ignore 规则，会被提交：`.integration-lab.log`、`lab-3010.log`、`lab-3010.err.log`、`wrangler-3000.log`、`wrangler-3000.err.log`、`.wrangler-preview.log`、`.wrangler-preview-error.log`。
- 未纳入 ignore 的临时目录：`.codex_tmp`、`.runtime`。
- 与 `DEPLOYMENT_READINESS.md` 第 6 条「确认仓库不包含运行日志、PID」直接冲突。
- **必要修改**：补全 `.gitignore` 的 `*.log`、`*.pid` 等规则；`hosting.json` 或含标识的配置应加 ignore 或移入环境变量。

### 1.5 `wrangler.jsonc` 的 D1 `database_id` 仍是占位值

- 值为 `00000000-…-0001`，部署前需替换为远程 D1 真实 ID（README/DEPLOYMENT_READINESS 已声明）。

### 1.6 健壮性 / 待确认项（中低）

- **无重放防护**：集成接口只靠 `eventId` 幂等去重，`occurredAt` 只校验格式不校验时效，无签名、时间窗或防重放。生产必须补。
- **无 CSRF/限流/告警**：写接口靠弱 header 认证 + CORS 兜底，无结构化运行日志、限流、告警（`DEPLOYMENT_READINESS.md` 已列为生产阻断项）。
- **时间计算混用时区**：`p0-flow.ts` 的 `liveTimeliness()` 用 `new Date().toISOString()` 与 `plannedEnd` 日期字符串直接比较，存在时区边界上的逾期/临期判定偏差。建议统一用明确时区。

> **小结**：核心问题不是代码质量，而是「演示身份/硬编码密钥」这套本地机制原样带到公网的边界。上线优先级：① 接真实认证授权 → ② 集成密钥进密钥托管并加签名/防重放 → ③ 清理 `.gitignore` 与占位 D1 配置 → ④ 补限流/告警/审计导出。

---

## 二、业务规则事实源矛盾（文档与代码契约对齐）

### 2.1 G2 门名、退出事件、决策人，权威基线 vs 代码契约不一致

- `LIFECYCLE_GATE_BASELINE.md` 第 3 节：G2 =「策略批准门」，退出事件 `StrategyApproved`，决策人「销售主管；重大项目升级 VP」。
- `app/domain/sales-project-contract.ts` 第 34 行：G2 =「投入启动门」，退出事件 `WorkInitiationApproved`，决策人「销售主管/授权人」。
- 代码 `DOMAIN_EVENTS` 里没有 `StrategyApproved`，只有 `WorkInitiationApproved`。
- `BUSINESS_FLOW_BASELINE.md` 阶段责任表里 S1 退出事件也还是 `StrategyApproved`。
- **结论**：从「策略批准」改「投入启动」是口径变更，但被声明为权威的 Gate 基线没同步。

### 2.2 G3 退出事件名三处不一致，决策人自相矛盾

- `LIFECYCLE_GATE_BASELINE.md` 第 3 节：G3 =「需求方案基线门」，退出事件 `CustomerRequirementBaselined`，决策人「技术负责人」。
- 同文件第 7 节 + `ACTIVITY_OWNERSHIP_BASELINE.md`：技术负责人编制、技术评审人独立确认 G3——决策人应是**技术评审人**。
- `app/domain/sales-project-contract.ts` 第 35 行：G3 =「需求与技术基线门」，退出事件 `TechnicalBaselineApproved`，决策人「独立技术评审人」。
- 同文件 `DOMAIN_EVENTS`（563 行）只有 `CustomerRequirementBaselined`，没有 `TechnicalBaselineApproved`。
- **结论**：退出事件在「文档 / 代码 STAGE_GATES / 代码 DOMAIN_EVENTS」三个地方出现两个名字；决策人「技术负责人」与「独立技术评审人」直接冲突（后者符合编制/评审分离原则）。

### 2.3 场景口径「三类 vs 四类」未同步

- `BUSINESS_FLOW_BASELINE.md` 场景编码表只有 SCN-01/02/03，原文反复说「三种场景」；`LEAD_BID_INTEGRATION_BASELINE.md` 也写「三种业务场景」。
- `app/domain/sales-project-contract.ts` 的 `SCENARIO_TYPES` 有四个，多出 `SCN-04-OVERSEAS-PARTNER-EPC`（海外伙伴/EPC），`ACCEPTANCE_MATRIX.md` 也用「四场景共通」。
- **结论**：规则从三类扩到四类是合理演进，但权威业务基线没同步。

### 2.4 G5 专业评审触发规则不对称，依据未闭环

- `app/domain/gate-scenario-policy.ts`：国内正式招投标（SCN-01）和海外伙伴/EPC（SCN-04）`g5ReviewRule: "always"` 强制三类评审；国内 EPC 询价（SCN-02）和国内直接询价（SCN-03）`g5ReviewRule: "conditional_pending_enterprise_rule"` 降级为非硬条件。
- **问题**：同样是 EPC 询价，海外强制、国内条件触发，这个不对称本身缺业务依据。文件只写「企业阈值未确认」，没解释海外为何「确认了强制」。建议补口径：海外强制是风险/合规默认，还是已确认制度。

### 2.5 G4 底价校验报错语义错位

- `p0-flow.ts` 第 1188 行：授权报价 < 授权底价时报错「低于底价必须先走价格例外授权」。但「低于底价走价格例外」是 S4 通路例外（`EpcPriceException`）规则，不是 G4 批准时动作。G4 阶段授权报价比底价低本质是授权数据自相矛盾，应直接拒绝重填，而非提示「走价格例外」。

### 2.6 机会查重指纹在「无招标编号」时粒度偏粗

- `buildOpportunityFingerprint`：有 `tenderNo` 时用「最终客户+项目名+招标编号+标包+产品范围」；无 `tenderNo` 时改用「交付地点 + 投标/报价月份」补位。后果是同一客户、同一交付地点、同一月份、产品范围相同的两个不同询价批次会被判重。建议至少补 `requestRef`（询价编号）进指纹。

### 2.7 已诚实标注、但上线前必须补齐的规则缺口（不算错，算未完成）

- 重招/多轮投标的生命周期重启规则（从 S 几重新进、是否新建项目）——`ACCEPTANCE_MATRIX.md`「待业务确认」。
- 附条件批准的完整状态机（条件责任人、到期、逾期处置）——`BUSINESS_FLOW_BASELINE.md` 明确「运行态不提供」。
- G5 不投/不报的批准权限、项目等级审批层级与金额阈值、各 Gate SLA——`LIFECYCLE_GATE_BASELINE.md` 第 9 节。
- G7 经营关闭阈值（履约/开票/回款/遗留责任的关闭标准）——当前正确做法是「受控未开放」，不伪造关闭。
- 线索 S/A/B/C 与项目评级是否长期同源——`CHANGE_BACKLOG.md`。

> **优先级**：先修 2.1–2.3 事实源矛盾（只动文档/契约常量），再补 2.4–2.6 规则依据说明，最后逐条确认 2.7 缺口。

---

## 三、产品/可用性改进（全局）

### 3.1 演示时间线硬编码，与真实时间脱节且无标注（最影响第一观感）

- `app/demo-data.ts` 所有 `bidDate`/`countdown`/行动 `due`/关系 `lastTouch` 写死 2026-08-XX；工作台标题写死 `2026年8月17日 · 星期一`；语音示例写死 `8月20日前`。
- 当前时间 9 月后打开会看到「7日内交标 / 距交标2天 / 最近仅剩2天」等冲突信息。
- **建议**：界面/README 标注「演示时间基线 = 2026-08-17」，或把 `countdown`/逾期判定改为相对当天动态算。

### 3.2 「AI 语音录入」纯 mock，抽取字段与输入无关

- `RoleDashboard.tsx` 的 `startVoice()` 是 `setTimeout` 900ms 后写死转写文本；客户事实/关系态度/下一行动/可能影响均为写死值，与口述/编辑内容不联动。
- **建议**：接真实 ASR+大模型，或改为随转写文本联动的规则抽取，并明确标注「模拟转写，非实时识别」。

### 3.3 高风险写操作无二次确认

- 中标/丢标确认（`onWin`/`onLost`）、G6 提交/批准、偏差批准均为点击即执行，无 confirm 或「不可撤销」提示。B 端惯例应加二次确认 + 后果文案。

### 3.4 术语提示（Term）在移动端/触屏不可达

- tooltip 用 CSS `:hover::after + content: attr(data-tip)`，触摸设备无 hover，解释基本看不到；纯 CSS tooltip 无法延迟、窄屏易溢出。建议改可点击/可聚焦弹层或 `aria-describedby`。

### 3.5 「AI 建议」标签名不副实

- 多处「AI建议/辅助建议」实为硬编码规则模板字符串。建议改标「规则建议 / 系统提示」，或明确「当前为规则引擎，非大模型」。

### 3.6 通知（toast）一次性、不可回看

- `notify()` 是 `setToast + 3200ms 清空`，无通知中心或历史。建议 toast 加「查看详情」或保留最近几条通知。

### 3.7 可选功能建议（非缺陷）

- 工作台首页无搜索入口，建议顶部放全局搜索。
- 首屏信息密度高、缺新手引导，建议加一次性 onboarding 或空态引导。

---

## 四、单项目详情页卡片级改进

> 按 8 个 tab 逐个卡片；★ 为优先处理项。

### 顶部 Hero（所有 tab 共享）

- **项目徽章组**：6 个徽章等权并列，「项目B级」与「线索B级」并排易混淆。建议等级合并为 `B级（由线索B级继承）`，徽章按「经营状态/事实来源」分组。
- **hero-stats 四宫格**：金额卡注明「多EPC只计一次」（好），但「赢单概率 %」未标注是经营判断值。建议概率卡加注「当前经营判断，随策略更新」，与事实类字段视觉区分。

### Tab 1 总览（OverviewWorkspace）

- ★ **健康三角 + 三图例卡**：三维度口径不一致（关系健康分 / 已到位角色数 / 已完成行动数），三角图把它们画成等权三角会误导。建议三角图只放三个同口径分数，到位数/行动数移到图例卡小字。
- **图例三按钮**：可点击跳转但视觉像图例。建议加 hover 态 + 箭头/「查看」字样。
- ★ **项目来源与采购关系折叠卡**：EPC 场景信息爆炸，「金额只计一次」关键规则被埋在最底部。建议拆成 2–3 个独立卡，该规则提为常驻提示条。

### Tab 2 关系（RelationsWorkspace）

- **客户血缘链卡**：空态无「下一步去哪」引导；「线索APP维护」是数据属主说明却占状态位。建议空节点补引导，属主说明改角标。
- ★ **关系证据与 Touch 记录表**：硬伤——「触达类型」列按 `index%2` 交替造假值，「证据」列是 `纪要-20260817.pdf` 这类假文件名且点不动。建议接真实数据或整列标「示例数据」并去掉可点击暗示。
- **关系覆盖环形图**：环中心放「关系健康度」，与三层覆盖率换算关系不透明。建议环中心改放「覆盖率 2/3 层」直接读数。

### Tab 3 策略（StrategyWorkspace）

- **S1 策略来源表单**：9 个 textarea 一屏铺开无分组，保存后只有一次性 toast。建议按「目标→判断→计划→风险/证据」分组，保存后即时刷新版本号。
- ★ **竞对对比表**：「服务」列是 `Math.round((关系+交付)/2)` 估算值，未标注；8 列窄屏溢出。建议估算值标「估」角标或淡色，小屏横向滚动/卡片折叠。

### Tab 4 资源（ResourceCapacity + PartnerGovernance）

- ★ **内部资源卡**：「模拟指派并接受」按钮直接改数据，违背「指派≠到位、须责任人接受」的核心规则。建议标注「演示模拟」或改走真实「下发→接受」流程。
- **伙伴治理卡**：多为「未判断/尚无候选」空态，占大卡价值低。建议空态收成引导行。

### Tab 5 行动（ActionsWorkspace）

- ★ **里程碑与双轨活动**：全站信息量最大，首次使用者难上手；依赖视图用 `→` 串联同轨活动，暗示严格顺序依赖，但活动可能并行、只有里程碑是锚点。建议箭头只连里程碑，加图注「活动可并行，里程碑是结果锚点」。
- **选中活动五段式**（对象引用→计划动作→责任人→时间状态→完成事件/证据）：本页最好设计之一，建议保留并可作为通用解释范式。

### Tab 6 版本（VersionsWorkspace）

- ★ **基线一致性主矩阵**：所有行默认折叠只露结论列；「投标响应」列几乎全空。建议偏差/需重评行默认展开，空列在 S5 前整列收掉或置灰。
- **版本对比卡**：因「源系统只回摘要」空转，没演示出对比能力。建议至少给一组示例 diff（标「示例」）。

### Tab 7 阶段门（GateWorkspace）

- **来源事实网格（G2/G3/G4/G5/G6）**：大量「来源缺失」不醒目；「硬条件 vs 非阻断上下文」靠文字区分。建议缺失项红边 + 「去哪补」按钮，就绪项绿色收拢，分组做视觉分区。

### Tab 8 结果（ResultWorkspace）

- **下游系统跟踪**：demo 全是「等待/尚未」。建议赢单基线生成前整块置灰标注「中标后才可见」。

> **一句话总结**：最该先改三处演示可信度硬伤——关系证据表假触达类型/假文件名、竞对表估算「服务」分、资源卡「模拟指派」按钮；其次是健康三角口径和一致性矩阵信息分层。

---

## 五、销售员视角使用摩擦

> 代入销售员「陈晨」动线；核心痛点是「点了有反馈、实际没结果」的假按钮。

1. **首屏认知负荷压住**：4 类筛选 + 任务/项目双视图 + 快捷记录 + 语音 + 动态 + AI 建议堆在一起。建议默认只展开「我现在要做」top 3，顶部加一句话今日结论。
2. **完成勾选框是死按钮**：任务行 `○` 点了只弹 toast「需要补充结果和证据后才能完成」，不跳转、无补证据入口。建议改成「去完成」，一键跳到补结果+证据表单。
3. **催办/补材料点了没产物**：只 toast「已生成催办草稿/已打开补充材料区」，实际无草稿、无跳转。建议真生成可复制文案并跳转，或改文案明确「仅记录诉求」。
4. **AI 语音录入说 A 记 B**：转写与抽取字段全部写死，与口述无关。建议至少让转写可编辑、抽取随编辑联动，或显著标注「模拟转写」。
5. **AI 建议只给一条且不可直接处理**：只显示 `actions[0]`，只能「查看依据」跳走。建议给 top 3 + 每条「处理」按钮。
6. **看不到「我的经营盘子」**：缺项目数/总金额/平均赢单概率/本周完成数/高风险数的聚合。建议首页加「我的经营概览」。
7. **「待内部响应/待客户」制造焦虑却推不动**：销售员做不了的事项挂在眼前，唯一能做的催办还是假按钮。建议视觉分层「我能推进」vs「我只能跟进/催办」，催办后降级为「跟进中」。
8. **时间线硬编码**：标题写死 `2026年8月17日 · 星期一`，倒计时写死「最近仅剩2天」，与真实日期冲突。建议先标「演示时间基线」。

> **一句话**：系统把「该被管住的合规动作」做得好，但把「顺手把活干完」的动作链做断了。优先级：②③④ 假反馈项先修，再补 ⑥ 经营概览和 ⑧ 时间标注。

---

## 六、销售主管视角使用摩擦

> 代入销售主管动线；「重决策」扎实，但「轻动作」全线是断的。★ 优先修。

1. ★ **待办队列「退回补充」是死按钮**：`RoleDashboard.tsx` 除 G1 外所有项点「退回补充」只弹 toast，Gate 状态没变成 `returned`，Owner 收不到。真正的退回入口在 `GateWorkspace` 的 `onReturn`，工作台待办没连到。建议直接打开对应 Gate 退回抽屉，或跳转到退回位。
2. ★ **待办只有「退回」+「进入项目」，无「去决策/去处置」直达**：G2/G4/G5/G6、资源、偏差缺主按钮。建议按类型给「去审批/去决策/去确认/去配置/去审批」一键落表单。
3. ★ **「发起资源协调」是假按钮**：`DemoApp.tsx` 只 toast「已生成并模拟下发」，无真任务。建议真生成可追踪任务，或改文案「仅演示」。
4. ★ **旧版资源卡「模拟指派并接受」直接置「已到位」**：违背「指派≠到位」核心规则。建议删掉 mock，统一走新版候选池「确认指派→待本人接受」流程。
5. **G3/G4 专业 Gate 督办无闭环**：点「督办」只弹「不能代替专业确认」，无对象/记录/回执。建议督办落可追踪记录。
6. **偏差审批入口断链**：待办有 `deviationWorkItems`，展开却只有假退回；进入项目也看不到明确偏差批准/退回按钮。建议给偏差项「去审批」直达，并在 versions tab 补批准/退回动作。
7. **风险视角只能看不能处置**：`rd-risk-overview-v2` 风险卡片只有「查看项目风险」，不能指定责任人/下发整改/标记接受。建议加处置动作，并与 G5「显式接受剩余风险」打通。
8. **缺团队人效/负荷聚合视图**：谁超负荷、谁闲置、关键角色缺口只能进单项目看。建议组合页加「关键角色负荷/缺口」总表。
9. **「待我处理」把「要我决策」和「要我督办」混一队列**：建议视觉分层区分，默认突出「需要我拍板」项。
10. **AI 建议只显示第一条**：`rd-agent-context` 只取 `workItems[0]`。建议 top 3 + 每条直达处理按钮。
11. **时间线硬编码**：`deadlineProjects` 按写死 `countdown` 算「7日内交标」，与真实日期失真。建议先标基线或动态算。

> **一句话**：主管视角「重决策」是全场最扎实的部分，「轻动作」——退回、督办、资源协调、偏差审批——大多是弹个 toast 完事。优先级：① 退回接真实退回 → ② 待办加「去决策/去处置」直达 → ③ 删两个假按钮 → ④ 补督办闭环 + 团队人效视图。

---

## 七、跨目录补充检查（集成实验室 + 操作工作区）

> 前几轮主要聚焦 `DemoApp.tsx / RoleDashboard.tsx / CoreWorkspaces.tsx`，本轮扩到 `app/OperationalWorkspaces.tsx`、`integration-lab/`、`app/api/_lib/integration-flow.ts`。

### 7.1 集成通道：身份由「前端自决」，且假哈希冒充不可变标识（最严重）

- **前端自决身份**：`integration-lab/src/main.tsx` 的 `send()` 按任务类型自动给自己分配 `contract-admin-liu`、`authorizer-luo`、`technical-reviewer-wang` 等身份；服务端 `app/api/_lib/integration-flow.ts` 的 `parseIntegrationEnvelope()` 只校验 actorId 是否在 12 个硬编码白名单里，不验真身。配合已知 `x-integration-key` 硬编码密钥，任何 curl 都能以「罗总」身份发 G4 价格授权、以「王评审」身份发 G3 评审结论，绕开职责分离。
- **假 SHA256 冒充不可变标识**：`integration-lab/src/main.tsx` 满是写死的 `SHA256:LAB-SOURCE-V1`、`SHA256:LAB-BID-PACKAGE-001` 等；更隐蔽的是 `packageHash: "SHA256:${task.external_task_id}-${routeId.slice(-8)}"`、`receiptHash: "SHA256:RCPT-${task.external_task_id}"`——用任务编号拼字符串，根本不是内容哈希。全站「冻结快照 + 哈希校验」的核心可信机制被做成摆设。

### 7.2 `OperationalWorkspaces.tsx`：六个操作工作区里有一批「假按钮」+ 写死资源数据

假按钮（点了只 toast、无产物无状态变更，与前述同类问题重复出现）：

- `ResourceCenter`「设为候选」→ 只 toast，没真写入候选。
- `ResourceCenter`「发起协调」→ 只 toast「已生成协调任务」，无真任务（与 DemoApp.tsx 第二处重复）。
- `RiskCenter`「生成督办」→ 只 toast「已生成督办草稿，尚未下发」，无真记录。
- `SalesActionCenter`「生成催办」→ 只 toast「已生成催办草稿」，无真草稿。
- `DecisionCenter`「筛选」按钮 → 只 toast，实际筛选由顶部 tabs 承担，按钮是空的。

写死数据违背项目自己「不伪造数据」原则：

- `ResourceCenter` 的 `pools` 人员全景全硬编码——赵工负荷 92%、available 日期写死 `8月23日/8月21日/8月20日/8月22日`，与当前时间冲突。其他卡片都强调空态不伪造，唯独这张「团队能力全景」是纯静态拍脑袋数据。

### 7.3 写死的评分/等级

- `integration-lab/src/main.tsx` 的 `sendLead()`：`score: 82`、`gradeAssessment.dimensions` 全 A、`ruleVersion: "LEAD-GRADE-DEMO-V0.3"`——线索五维评估是写死值。
- `scripts/import-company-lead-fixtures.mjs`：`authenticityAssessment` 写死 5/4/4/4/4=21/25、`gradeAssessment` 写死 B/A/A/A/B、`ruleVersion: "HTML-PROTOTYPE-V1"`。脚本本身是注入演示样本，性质可接受，但建议明确标注「样本值，非真实评估」。

### 7.4 没有问题的部分

- `worker/index.ts`：Cloudflare 图片优化代理，无业务逻辑。
- `examples/d1/`：D1 模板示例，非业务核心。
- `scripts/cleanup-local-acceptance-data.mjs`：清理脚本有 `--apply` 保护（默认只读预览），设计谨慎。

---

## 附：整体优先级建议（汇总）

1. **生产上线前硬阻断**（第一部分）：接真实认证授权、集成密钥进密钥托管、清理 `.gitignore` 与 D1 占位、补限流/告警。
2. **业务规则事实源矛盾**（第二部分 2.1–2.3）：先统一 G2/G3 事件名、G3 决策人、三/四场景口径。
3. **演示可信度硬伤**（贯穿三、四、五、六、七部分）：关系证据表假值、竞对估算分、模拟指派、语音 mock、假哈希、写死资源数据。
4. **假按钮 / 动作链断裂**（第五、六、七部分）：完成、催办、补材料、退回、督办、资源协调、偏差审批——「点了有反馈、实际没结果」。
5. **可用性细节**：时间线标注、二次确认、术语 tooltip、AI 建议多条目、通知历史、经营概览/人效视图。
